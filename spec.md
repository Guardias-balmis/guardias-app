# spec.md · Especificación de dominio de guardias-app v2

> Artefacto versionado y diffable. Fuentes de verdad: `docs/normativa.pdf` (v2.0) y
> `docs/guardias_radiodiagnostico_balmis_v2.xlsm`. Si esta spec contradice la normativa, gana la
> normativa y la spec tiene un bug. Arquitectura: `docs/adr/001-arquitectura.md`.

## 1. Principios

1. **Derivar > almacenar.** Todo lo computable desde fechas se computa; el nivel R1–R4 no existe como dato.
2. **La identidad es un UUID.** Nunca la posición, nunca el nombre, nunca el email (ambos pueden cambiar).
3. **Nunca se borra un residente.** `activo` es derivado; el historial es sagrado.
4. **El dominio es puro.** Cero I/O, cero React, cero Sheets. Mismo código en cliente y servidor.
5. **Fechas como strings ISO `YYYY-MM-DD`** y aritmética en UTC. Meses **1–12**. (El cliente v1 tiene un bug de desfase +1 mes por usar índices 0-based de JS; esta clase de bug queda prohibida por convención.)

## 2. Modelo de dominio

```
Residente {
  id: UUID                      # estable para siempre
  nombre: string                # mutable, solo presentación
  email: string                 # mutable, clave de login (comparación case-insensitive)
  fechaInicioResidencia: date   # p.ej. 2024-05-07 — los desfases respecto a junio son NORMALES
  fechaFinResidencia: date      # normalmente inicio + 4 años − 1 día; puede alargarse (bajas)
}
PeriodoFormativo {              # 4 por residente; generados por defecto, editables (nota [a] normativa)
  residenteId, anio: 1..4, fechaInicio: date, fechaFin: date
}
Bloqueo {
  id: UUID, residenteId
  desde: date, hasta: date      # RANGO inclusivo, no un día suelto (decisión V-6)
  tipo: mixto                   # solo BAJA bloquea la asignación (INV-5); VACACIONES/ROTACION
                                 # ya no bloquean, son informativas, pero siguen alimentando
                                 # INV-2/6/7 y la equidad (decisión V-8, Fase 5.x)
  motivo: VACACIONES | ROTACION | BAJA   # embarazo/paternidad → BAJA (decisión V-5)
  provincia?: string            # solo ROTACION: Alicante/Valencia/Murcia/Albacete activa INV-7
  guardiasEnCentroExterno?: bool
  activo: bool                  # cancelación = reinserción con mismo id y activo=false (append-only)
}
# Pendiente de modelar (§5 INV-13): Imaginaria (dos listas rotatorias por grupo) y la validación
# del Responsable (nivel R3, sorteo reproducible). Ver Estado de implementación (§7).
Asignacion {
  fecha: date, residenteId, codigo: G|GF|GP|3P, puesto: MAYOR|PEQUENO|TERCERO
  origen?: CEDIDA | COMPRADA    # si existe, se excluye del cómputo anual (INV-4)
}
Cuadrante { mes: 1..12, anio, estado: BORRADOR|VALIDADO|PUBLICADO, actorId, fecha }
# actorId/fecha (Fase 6.2, decisión V-10): quién disparó la ÚLTIMA transición y cuándo — no un
# campo por tipo de transición (generadoPor/validadoPor/...): cada fila `cuadrantes` es append-only,
# así que el historial completo de quién hizo qué transición ya vive en las filas anteriores.
Responsable { periodoInicio, periodoFin, residenteId, metodo: VOLUNTARIO|SORTEO,
              voluntarios[], semilla?, candidatos[]?, fechaSorteo? }   # sorteo reproducible y auditable
Generacion {                    # bitácora de un intento de generar el mes con IA (decisión V-45)
  id: UUID, mes: 1..12, anio, fecha: date, actorId
  modelo: string                # el id de modelo REALMENTE usado, no el del defecto del código
  intentos: 0..3                # cuántas vueltas del ciclo propuesta→validación hicieron falta (0: no se llamó al modelo)
  resultado: APLICADO | REVISION_MANUAL | ERROR_MODELO | CONFLICTO | FIJADAS_INVALIDAS
  # CONFLICTO (2026-09-04): la propuesta era válida pero el mes cambió (alguien guardó celdas,
  # registró una ausencia o movió su estado) mientras el modelo respondía; se volvió a juzgar bajo
  # el lock con los datos de ahora, no se escribió nada y hay que volver a pedirla.
  # FIJADAS_INVALIDAS (2026-09-04, solo en COMPLETAR): las guardias ya puestas en la rejilla
  # incumplen por sí solas una regla dura (o son de alguien que no está en activo ese día); no se
  # llamó al modelo (intentos=0) y hay que corregirlas en el cuadrante antes de generar.
  violaciones: json             # las que quedaban en el último intento (los avisos, si se aplicó); acotadas a 50 y 40.000 caracteres
  modo: COMPLETAR | REEMPLAZAR  # V-47: si respetó las guardias que ya había en la rejilla o sustituyó el mes
  # Append-only como todo lo demás. Es la ÚNICA constancia de que un mes lo propuso un modelo y de
  # que hubo un mes que no supo resolver: `asignaciones` guarda las filas, no quién las escribió.
  # REVISION_MANUAL es la «marca para revisión manual» del encargo: no se escribe ni una fila del
  # cuadrante, y queda dicho en el Sheet que ese mes hay que montarlo a mano.
}
Excepcion {                     # documenta que un aviso concreto está justificado (decisión V-29)
  tipo, desde: date, hasta: date, justificacion, registradaPor, fecha: date, activo: bool
  # `tipo` identifica QUÉ lee el dominio, no un INV-n suelto: hoy solo "2xR2" (INV-9), lista
  # blanca en el router. Un invariante podría tener más de un tipo de excepción el día de mañana.
}
```

**DURO vs BLANDO (decisión V-6, Fase 4; refinada por V-8, Fase 5.x):** `Bloqueo` y una
preferencia PERSONAL siguen siendo entidades DISTINTAS, no dos tipos de una misma tabla. Pero
dentro de `Bloqueo` la severidad ya no es uniforme por entidad, depende del `motivo` (V-8):
**BAJA sigue siendo DURA** — INV-5 prohíbe asignar guardia esos días, por seguridad/legalidad
(no se puede exigir una guardia a alguien de baja médica o embarazo). **VACACIONES y ROTACION
dejaron de bloquear la asignación**: son informativas para el generador, igual que una
preferencia BLANDA — pero sus fechas SIGUEN alimentando INV-2 (exención del mínimo mensual),
INV-6 (ausencias simultáneas por cohorte) e INV-7 (cobertura viernes/sábado en rotación
cercana): no se volvieron irrelevantes, solo dejaron de bloquear la asignación en sí. Una
preferencia PERSONAL ("preferiría no este día") sigue viviendo en `Preferencias.fechasEvitar`
(fechas concretas sueltas, no un rango), informativa y **nunca** comprobada por el validador.
`Preferencias.fechasPreferidas` (BLANDO positivo, "quiero guardia aquí") se retiró en Fase 5.x
a petición del autor: no aportaba suficiente valor frente a la complejidad de mantenerlo.

## 3. Reglas derivadas (funciones puras)

### 3.1 Periodos por defecto — `defaultTrainingPeriods(inicio, fin)`
- Periodo k (1..3): `[inicio + (k−1) años, inicio + k años − 1 día]`. Periodo 4: `[inicio + 3 años, fin]`.
- Aniversarios 29-feb se ajustan a 28-feb en años no bisiestos.
- Editables después (bajas/embarazo, nota [a]): se permiten huecos entre periodos (promoción
  retrasada), **nunca solapes**.

### 3.2 Nivel — `nivel(periodos, fecha)`
- `PENDIENTE` si `fecha < periodos[0].fechaInicio`.
- `FINALIZADO` si `fecha > último.fechaFin`.
- Si no: `R{k}` del **último periodo cuyo inicio ≤ fecha** (en un hueco entre periodos se conserva
  el nivel anterior: una baja retrasa la promoción, no des-promociona).
- Consecuencia: cada residente sube en **su aniversario** (en la práctica, mayo); en junio ya han
  subido todos → cumple DoD-2 sin cron. Un residente con `FINALIZADO` deja de listarse por cálculo
  y su historial queda intacto.

### 3.3 Grupo — `grupo(nivel)`
`R3|R4 → MAYOR`, `R1|R2 → PEQUENO`, resto → `null` (no asignable).

### 3.4 Calendario
- Día de semana desde la fecha ISO (L,M,X,J,V,S,D); fin de semana = S|D.
- **Año académico** de una fecha: `mes ≥ 6 → año`, si no `año − 1` (jun-2026→may-2027 ⇒ 2026).
- **Trimestre**: T1 jun-ago, T2 sep-nov, T3 dic-feb, T4 mar-may (T3 cruza el año natural).
- **Festivos**: son *datos de entrada* (lista de fechas por año, la carga el responsable). Nunca se
  calculan ni se delegan a la IA (el cliente v1 le pedía al modelo "identifícalos tú": prohibido).
- **Puente**: día laborable (L–V, no festivo) cuyos dos vecinos son cada uno festivo o fin de semana.
  (Cubre: viernes tras jueves festivo; lunes ante martes festivo.)

## 4. Semántica del contaje — `contaje(asignaciones, ventana, festivos)`

Replica la semántica del Excel salvo mejoras documentadas. Devuelve por residente:

| Métrica | Definición | Fórmula Excel equivalente |
|---|---|---|
| `total` | nº de G + GF + GP | `O = C + D + M` (Resumen Anual) |
| `findes` | guardias (G/GF/GP) en sábado o domingo | `SUMPRODUCT((S∨D)·guardia)` |
| `festivos` | nº de GF | columna GF |
| `prefestivos` | nº de GP | columna GP |
| `dobletes` | viernes con guardia **y** domingo (+2 días) con guardia | `SUMPRODUCT((V)·g(d)·g(d+2))` |
| `tercerPuesto` | nº de 3P | columna 3P |
| `cedidasCompradas` | nº de asignaciones con `origen` (informativo) | — |

- **Contaje "tonto a propósito" (decisión T-1):** `tally(asignaciones, ventana)` cuenta códigos por
  fecha y **no recibe la lista de festivos**: `festivos` se deriva del código GF, no de un calendario.
  La coherencia código-vs-festivo oficial es el invariante **INV-12** (§5, pendiente), no la
  responsabilidad del contador (evita "corregir" en silencio y desincronizarse del Excel de referencia).
- **`puentes` NO es una métrica del contaje.** La normativa exige equidad de **puentes libres**
  (días puente en los que el residente **no** hace guardia) — una métrica de *ausencia*, no de guardia.
  Se computa en el validador con contexto de calendario (INV-3), no en `tally`. *(Corrección de la
  puerta de consistencia: la revisión 1 de esta spec confundía "guardias en puente" —que nadie
  necesita— con "puentes libres".)*
- **INV-4:** asignaciones con `origen` CEDIDA/COMPRADA y las 3P **no** entran en `total` ni en las
  métricas de equidad; se registran aparte (contadores `tercerPuesto` y `cedidasCompradas`). Una
  guardia comprada **no puede formar la mitad de un doblete**.
- **Doblete en borde de mes** (viernes 31-jul → domingo 2-ago): el dominio lo computa por **fechas
  reales** y lo atribuye **al mes del viernes**. *Mejora deliberada sobre el Excel*, que al operar
  por columnas de mes pierde estos dobletes y distorsiona la equidad anual. La proyección a Sheets
  (Fase 7) documentará la discrepancia de borde de sus fórmulas degradadas.
- **La ventana es del residente** (su año de residencia, aniversario→aniversario), no el año
  académico. El validador recibe el acumulado del año en curso: la normativa exige compensar entre
  meses ("quien asuma más guardias un mes lo vea compensado en los siguientes").

## 5. Invariantes — `validateMonth(cuadrante, contexto) → violaciones[]`

`violacion = {invariante, severidad: error|aviso, fecha?, residenteId?, detalle}`.
Un cuadrante con violaciones `error` no puede pasar a `VALIDADO`. Una `Excepcion` registrada no
degrada una severidad guardada en ningún sitio (no hay una columna "Excepciones" ni un estado
DURA/AVISO previo): el invariante que la consulta —hoy solo INV-9— la comprueba en el momento y,
si hay una que cubra la fecha con el `tipo` correcto, deja de emitir esa violación por completo.

Severidad `error` **bloquea** el paso a `VALIDADO`; `aviso` informa pero no bloquea (decisión V-4).
Estado: ✅ implementado y con test · ⏳ pendiente (fase indicada).

| # | Regla operativa | Ámbito | Severidad | Estado |
|---|---|---|---|---|
| INV-1 | Cada día: idealmente 1 MAYOR y 1 PEQUENO (por nivel derivado a esa fecha); 0 personas nunca es admisible | día | error (**ninguna** asignación ese día, o composición ≥2 incorrecta); **aviso** (cubierto por 1 sola persona, decisión V-12; asignado a quien no es asignable en esa fecha, decisión V-21) | ✅ |
| INV-2 | 4 ≤ guardias computables/mes ≤ 6 por residente activo | mes | **aviso** en las dos direcciones (decisión V-14: la normativa llama a estas cifras «orientativas»); exento del mínimo en febrero/vacaciones/baja/R1-verano | ✅ |
| INV-3 | **Cierre anual** (año de residencia): dif ≤ 1 con cada compañero del mismo año en total, findes, festivos, **prefestivos**, puentes libres y dobletes V-D. **Cierre trimestral** (ago/nov/feb/may): dif ≤ 1 solo en **total** | año personal · trimestre | **aviso** en los dos cierres (decisión V-14: la equidad nunca bloquea) | ✅ (prefestivos tras la puerta de consistencia; cierre trimestral en P-8; `puentesLibres` derivado de `festivos` en la fase 3 de V-17 — antes comparaba ceros) |
| INV-4 | 3P/cedidas/compradas fuera del cómputo | contaje | — (no emite violación: se aplica excluyendo en `tally`) | ✅ vía `tally`, y desde V-30 alcanzable desde la app: mantener presionada una celda con guardia en `Calendar.jsx` marca `origen` CEDIDA/COMPRADA |
| INV-5 | Ninguna asignación sobre Bloqueo motivo BAJA (vacaciones/rotación ya no bloquean la asignación, decisión V-8). Una ausencia con fecha **ilegible** no se juzga: se aparta y se reporta nombrándola (decisión V-22) | día | error | ✅ |
| INV-6 | Máx. 2 residentes de la misma promoción en rotación externa simultánea; rotación prioritaria sobre vacaciones | día | **aviso** (V-14: las ausencias ya están concedidas, el cuadrante no puede deshacerlas) | ✅ |
| INV-7 | Rotación en Alicante/colindante → ≥1 guardia en viernes **o** ≥1 en sábado dentro del periodo (disyunción, P-6) | periodo | **aviso** (V-14; evaluado solo en el mes de fin) | ✅ (ver contrato C-2; disyunción en vigor desde P-6, 2026-08-06) |
| INV-8 | 3P: cubrir L→D antes de repetir día (por voluntario); dif ≤ 1 entre voluntarios al cierre; prioridad a días con R1 de mochila | año | **aviso** en las cuatro reglas (decisión V-18: 8a y 8b bajan de error; 8c ya lo era por V-14 y 8d por V-4) | ✅ (en vigor desde V-18: tabla `voluntarios3P` con permanencia de 4 meses, `thirdPostHistoryStart`, `client/lib/thirdpost.js` y cableado en `marcarValidado` y `Calendar.jsx`) |
| INV-9 | 2×R2 el mismo día: solo desde el 1-dic del año académico con Excepcion justificada, **o** en un día de evento | día | **aviso** si no justificado (V-14: su causa no vive en el cuadrante del mes) | ✅ (fix P0-3: los eventos eximen con independencia del mes; Excepcion registrable desde la app desde V-29) |
| INV-10 | Navidad y despedida: 2 R2 por sorteo documentado; los de Navidad libres en la despedida | evento | **aviso** (V-4) | ✅ (en vigor desde V-20: tabla `eventos`, sorteo reproducible en `sorteos`, y las filas entrando por `buildMonthContext`) |
| INV-11 | Junio–agosto: ningún R1 asignado; recuento de Pequeños entre R2 del mismo año (dif ≤ 1, compensable) | mes | error (R1 asignado); **aviso** (dif entre R2) | ✅ |
| INV-12 | Coherencia código↔festivo: GF solo en día festivo; G/GP no en festivo | día | aviso | ✅ (tabla `festivos` + `calendar.isHoliday`/`bridgesOfMonth`; decisión V-17) |
| INV-13 | Imaginaria: dos listas rotatorias por grupo; la sustitución de una incidencia sale de la lista del grupo del incidente; una guardia de incidencia cedida/comprada NO descuenta de la imaginaria | evento | **aviso** (decisión V-20, baja de `error`) | ✅ como **herramienta** (`imaginaria.js` deriva la cola; tabla `imaginaria` registra las coberturas). NO lo comprueba `validateMonth`: no hay validación a posteriori, a propósito |
| INV-14 | Responsable: nivel R3 en su periodo (enero→enero); método SORTEO solo sin voluntarios; sorteo reproducible | año | error | ✅ (Fase 5, decisión V-7) |
| INV-15 | Descanso tras guardia: ningún residente hace guardia dos días consecutivos (cuenta el 3P; el par que cruza el borde del mes también) | día | error | ✅ (decisión V-35) |

### 5.1 Procedencia normativa de cada invariante

Cada invariante declara de dónde sale su autoridad. Existe porque en julio de 2026 aparecieron en
`docs/` ocho documentos de propuesta que discutían las mismas reglas sin citar ni una vez un `INV-n`,
apoyándose en una numeración propia (`RN-nn`, `CN-nn`, `RF-nn`) y en documentos que no existen en el
repo: era imposible saber si una propuesta corregía la normativa, la reinterpretaba o la ignoraba.
Regla derivada: **una regla nueva se cita contra `docs/normativa.pdf`, o se declara explícitamente
como extensión sin respaldo normativo** (decisión libre del autor, igual de legítima, pero marcada).
Recordatorio de §0: si esta spec contradice la normativa, gana la normativa.

- **INV-1** — normativa p.1 §Organización: «Hay dos puestos de guardia, uno de Residente Mayor (R3 y
  R4) y otro de Residente Pequeño (R1 y R2). No puede haber dos residentes mayores ni dos pequeños,
  salvo en las excepciones previstas en este documento». La degradación a aviso del día cubierto por
  1 sola persona es decisión V-12, no normativa. La frase de p.1 que divide a los residentes en
  «2 grupos» y dice que «cada grupo elaborará un cuadrante provisional» describe **cómo se
  reparten las guardias** (siempre un Pequeño con un Mayor) y cómo se organiza cada grupo, no dos
  unidades de datos distintas: el cuadrante mensual sigue siendo uno solo, en una sola pestaña
  (decisión V-15, que cierra P-7).
- **INV-2** — p.1: «el mínimo de guardias por residentes debe ser 4 y máximo de 6 guardias al mes»,
  con el «salvo excepciones (por ejemplo febrero o vacaciones)» que funda la exención. La severidad
  `aviso` (nunca error) sale de p.2, que degrada estas cifras explícitamente: «Estas cifras son
  orientativas. El criterio obligatorio será [la equidad]» — decisión V-14.
- **INV-3** — p.1 funda tres ejes: «diferencia máxima de 1 guardia total, 1 fin de semana y 1
  festivo»; **prefestivos** sale del recuento exigido en p.1 («totales, fines de semana, prefestivos
  y festivos»); **dobletes** de p.1 («un número equivalente de fines de semana dobles […] diferencia
  máxima de 1»); **puentes libres** de p.4 («un número equivalente de puentes libres, con una
  diferencia máxima de 1»). La ventana «año de residencia» es literal de p.2. El **cierre
  trimestral** (P-8) sale de la frase de p.2 que la propia normativa llama obligatoria: «Estas
  cifras son orientativas. El criterio obligatorio será que, una vez cerrado el cuadrante
  trimestral, la diferencia de número de guardias entre residentes del mismo año no supere 1, y
  que quien asuma más guardias en un mes vea compensado ese exceso en los meses siguientes hasta
  equilibrar el cómputo dentro del año de residencia». De esa frase salen las dos diferencias con
  el cierre anual (decisión V-13): mide solo `total` («número de guardias» — los demás ejes los
  ancla la normativa al año, p.1/p.4) y avisa en vez de bloquear (la misma frase prevé la
  compensación en los meses siguientes).
- **INV-15** — **Extensión sin respaldo normativo** (decisión V-35, 2026-08-10). `normativa.pdf` no
  regula el descanso posterior a la guardia en ninguna de sus 4 páginas: no hay frase que citar, y
  las dos propuestas de `docs/` que rozan el tema (P-1, pesos de descanso; P-9, horarios de
  guardia) están juzgadas «En contra» y «Ausente» respectivamente. La autoridad es del autor y es
  **legal, no normativa**: tras una guardia de 24 h corresponde el descanso del día siguiente, así
  que encadenar dos es ilegal con independencia de lo que diga el reglamento del servicio. Por eso
  es `error` y por eso NO es una excepción a V-14, que bloquea «lo imposible y lo **ilegal**» — es
  el mismo cajón que INV-5 (no se le puede exigir una guardia a quien está de baja), no el de
  INV-2. El 3P cuenta: la normativa lo llama «tercer puesto de guardia» (p.2) y el descanso legal
  no distingue si la noche era voluntaria.
- **INV-4** — p.2: «El conteo anual no incluye: tercer puesto de guardia ni guardias
  cedida/comprada». **Extensión sin respaldo normativo** (fase 3 de V-17): en el eje
  `puentesLibres` un **3P** en un día puente NO se lo quita a quien lo hace, y una guardia con
  `origen` (cedida/comprada) **sí**. La normativa no dice nada de esto —§4 deja `puentesLibres`
  fuera del contaje, así que la exclusión literal de INV-4 no la alcanza—, y la asimetría es una
  decisión: el 3P es voluntario (p.2, «será siempre voluntario»), de modo que quien lo hace
  renuncia él a su puente y el eje mide si **el reparto** se lo quitó; la cedida/comprada, en
  cambio, es una fila de guardia como cualquier otra y quien la tiene trabaja ese día.
- **INV-5** — **sin cita literal.** La normativa solo trata la baja en la nota [a] de p.2, y para
  descontar disponibilidad («se descontará de forma proporcional»), no para prohibir la asignación.
  Que BAJA bloquee es decisión V-5/V-8, fundada en seguridad y legalidad, no en la normativa.
- **INV-6** — p.2: «no ausentarse más de dos residentes del mismo año simultáneamente, teniendo en
  cuenta que no coincidan con periodo vacacional y siendo prioritarias sobre las vacaciones».
- **INV-7** — p.2: «se deberá realizar al menos una de las guardias de viernes y sábado que
  correspondan al periodo». ⚠️ **Redacción ambigua en la propia normativa**: «al menos una de las
  guardias de viernes y sábado» admite leerse como *una de entre {viernes, sábado}* (disyunción) o
  como *una de viernes y una de sábado* (conjunción). El código implementaba la conjunción; desde
  P-6 (§8, decisión directa del autor, 2026-08-06) implementa la **disyunción**. La distinción
  `provincia` (Alicante/colindante = rotación "cercana", la única que activa INV-7) se mantiene sin
  cambios: el autor confirmó que rotar en Alicante sigue contando como disponible para el reparto
  de guardias.
- **INV-8** — p.2-3: voluntariedad («será siempre voluntario»), lista rotatoria L-D antes de repetir
  día (con el ejemplo del domingo), «la diferencia máxima de 3.º puestos acumulados entre estos
  voluntarios no supere 1 al final del año de residencia», y la prioridad de mochila: «Los Residentes
  que hagan tercer puesto siempre deben cubrir primero los días que haya un R1».
  **Extensión sin respaldo normativo** (decisión V-18), dos puntos: (1) el **compromiso de
  permanencia de 4 meses** al apuntarse — la normativa dice cuándo el 3P es voluntario, no
  cuánto dura el voluntariado; los 4 meses y el alta en la fecha que uno quiera son criterio del
  autor sobre la práctica real («flexible a medias»), fundados en que sin continuidad la rotación
  L-D queda a medias; y (2) que el **ciclo de siete días arranque en la fecha de alta** de cada
  residente y se reinicie si se retira y vuelve a apuntarse. La normativa describe la rotación
  («no repetir día hasta completar el ciclo») pero no dice desde cuándo se cuenta; anclarla al
  alta es lo que hace que un 3P suelto de hace dos años no condicione el ciclo de hoy.
- **INV-9** — p.3: «a partir de diciembre-enero, nunca antes», «carácter excepcional y deberán estar
  justificadas por necesidades organizativas objetivas», y el cierre de equidad «no difiera en más
  de 1 respecto a sus compañeros del mismo año».
- **INV-10** — p.3: «Se cubrirá por dos R2, a sorteo, salvo que alguien no quiera acudir […] Los
  designados para Navidad estarán libres en la despedida».
- **INV-11** — p.1: «En junio, julio y agosto, dado que se organizan sin los R1, el recuento se hará
  en el grupo de Residentes pequeños (R1 y R2), entre los residentes del mismo año, de manera que no
  superará 1 guardia de diferencia».
- **INV-12** — **sin cita literal.** La normativa no define coherencia código↔festivo en ninguna
  parte; es una comprobación de consistencia interna derivada de S-4 (los festivos son datos de
  entrada). Extensión sin respaldo normativo, aplazada.
- **INV-13** — **parcialmente sin respaldo normativo** (decisión V-20). La normativa funda las dos
  listas, el criterio «lista del grupo de la incidencia» y que la cesión/compra no descuente. **No
  están en ella** y son práctica real del servicio: el orden de la cola (por cobertura más
  antigua), la **exclusión del R1** de la lista de Pequeños —excepción a la agrupación de INV-1 e
  INV-11, solo aquí— y la exclusión de quien tenga guardia la víspera o el día siguiente. Tampoco
  hay ninguna exigencia de equidad: a diferencia de INV-3, INV-8 y los dobletes, la Imaginaria es
  cobertura de emergencia y no un eje de reparto. Cita literal a continuación.
- **INV-13** — p.4: «Se dispone de dos listas de Imaginaria. Una para residentes mayores y otra para
  residentes pequeños […] La cesión/compra de guardia de incidencia NO descuenta de la imaginaria».
- **INV-14** — p.2: «recae sobre un R3 de enero a enero de R4 […] Está designado por sorteo en
  ausencia de voluntarios».

### Contratos del validador (verificados por la puerta de consistencia)
- **C-1 (lookahead de dobletes, ref. S-5):** cualquier cómputo mensual de `dobletes` que luego se sume
  (p. ej. la proyección a Sheets de la Fase 7, o los acumulados de `equity`) debe pasar a `tally` las
  asignaciones **más los ~2 primeros días del mes siguiente**, porque el domingo de un doblete de un
  viernes-31 cae fuera del mes. `tally` hace el *lookahead* correctamente **si el dato está presente**;
  omitirlo reintroduce el bug del Excel que S-5 corrige. `validateResidencyYearClose` es correcto para
  el mes de cierre (el domingo posterior al aniversario pertenece al año siguiente).
- **C-2 (periodo de INV-7):** `validateMonth` evalúa INV-7 solo en el mes en que **termina** la rotación,
  y para ello `asignaciones` debe incluir las guardias del residente **de todo el periodo** de rotación
  (que puede abarcar meses anteriores), no solo las del mes validado.

## 6. Decisiones registradas

| # | Decisión | Motivo |
|---|---|---|
| V-1 | **Reconciliación INV-1/INV-9:** un día con 2 Pequeños **ambos R2** lo gobierna INV-9 (excepción 2×R2); cualquier otro día defectuoso (2 mayores, R1+R2, falta puesto) es INV-1 | Los agentes de diseño de tests etiquetaron la misma situación con códigos distintos. El comportamiento aceptar/rechazar es el de la normativa; solo se unifica la etiqueta. La excepción 2×R2 solo se plantea cuando ambos son exactamente R2 |
| V-2 | **Cohorte (promoción) = año natural de `fechaInicio`**; distinta del **nivel** (derivado por fecha). INV-6/INV-11 comparan por cohorte; el rol Mayor/Pequeño usa nivel | El caso del aniversario de mayo: tres residentes de la promoción 2024 siguen siendo "el mismo año" aunque su nivel computado difiera unos días |
| V-3 | Severidades `error` (bloqueante) / `aviso` (informativo) | Alineado con la salida del validador; mapea DURA→error, AVISO→aviso |
| V-4 | **Clasificación de severidad** (tras la puerta de consistencia): `error` protege equidad/seguridad/legitimidad; `aviso` es advisory/compensable/social. Aviso: INV-2 (Pequeño con 3), INV-8d (mochila), INV-10 (eventos), INV-11 (dif mensual entre R2), INV-12 (coherencia festivo). El resto, `error` | La normativa dice "cifras orientativas, el criterio obligatorio es la equidad": un cuadrante no debe bloquearse por reglas blandas. El mecanismo `aviso` no existía y varias reglas blandas bloqueaban cuadrantes válidos |
| V-5 | Embarazo/paternidad se modela como `Bloqueo` motivo **BAJA** (para el descuento proporcional de la nota [a]) | Evita ampliar el enum; el descuento de disponibilidad es idéntico |
| V-6 | `Bloqueo` es rango `desde`/`hasta` (no un día suelto) y siempre DURO; BLANDO no es un `Bloqueo`, es `Preferencias.fechasEvitar` (fechas sueltas, no enforced) | Corrige la spec para que coincida con `validate.js` (ya implementado y probado E2E en Fase 3-4): INV-6/7 operan sobre periodos, no días sueltos; colapsar DURO/BLANDO en una tabla habría mezclado "prohibido" con "preferiría no" |
| V-7 | Responsable (Fase 5): (a) la `semilla` del sorteo la genera la app (no una fuente externa), pero el sorteo es puro/determinista sobre (candidatos, semilla) — recomputable a partir del registro guardado, y cambiar cualquiera de los dos a posteriori cambia el resultado (detecta manipulación); (b) si se ofrecen ≥2 voluntarios (la normativa solo cubre "sorteo en ausencia de voluntarios"), se sortea SOLO entre ellos, no entre todo el grupo de R3; (c) candidatos = residentes con nivel R3 exactamente en `periodoInicio` (1 de enero), derivado, nunca almacenado aparte | Respuestas del autor durante la Fase 5. (a) prioriza simplicidad de implementación sobre auditabilidad por terceros sin acceso a la app — aceptado conscientemente. (b) caso no cubierto por la normativa; se prefirió reutilizar el mismo mecanismo de sorteo antes que "gana el primero en ofrecerse" (más débil de auditar) |
| V-8 | Bloqueo (Fase 5.x): la severidad depende del `motivo`, ya no es uniforme. BAJA sigue bloqueando la asignación (INV-5 sin cambios para ese motivo). VACACIONES y ROTACION dejan de bloquear: no producen ninguna violación (igual que `fechasEvitar`), pero sus fechas siguen alimentando INV-2 (exención <4 guardias/mes), INV-6 (ausencias simultáneas por cohorte) e INV-7 (cobertura viernes/sábado en rotación cercana) y el descuento proporcional de equidad — nunca dejaron de ser datos relevantes, solo dejaron de bloquear la asignación en sí. `Preferencias.fechasPreferidas` (BLANDO positivo) se retira de la pantalla y del esquema. Corrige además una contradicción real preexistente: INV-7 exigía al menos una guardia de viernes y una de sábado *dentro* del periodo de rotación cercana, mientras INV-5 prohibía *cualquier* asignación en ese mismo periodo (rotación era DURO) — nunca detectada porque los tests de INV-7 no comprobaban INV-5 en el mismo escenario | Petición del autor: una guardia sobre vacaciones/rotación no debería bloquearse en la app — "muy a nuestro pesar, debería ponerse guardias esos días" si no hay alternativa. Baja médica se mantiene DURA por seguridad/legalidad (no se puede exigir una guardia a alguien de baja): decisión explícita del autor, no un default heredado |
| V-9 | Fase 6 (generador): (a) se trocea en **6.1** (prompt con contaje acumulado + bloqueos reales inyectados, y validación multi-mes correcta — contrato C-2) y **6.2** aparte (ciclo de estados BORRADOR→VALIDADO→PUBLICADO); (b) en Fase 6, PUBLICADO es un estado **interno de la app** (bloquea ediciones del mes) — la proyección real a la pestaña del Sheet legible por humanos queda para la Fase 7, tal y como ya preveía este documento; (c) solo el Responsable R3 en su periodo (rol de Fase 5) puede pasar un cuadrante de VALIDADO a PUBLICADO | Respuestas del autor antes de empezar la Fase 6: trocear reduce el riesgo por entrega, mismo patrón que 4.0/4.1-4.2 y 5/5.x; reutilizar el rol Responsable evita inventar un permiso nuevo; separar PUBLICADO=interno de PUBLICADO=Sheets evita mezclar el ciclo de estados de la Fase 6 con el trabajo de proyección real de la Fase 7 |
| V-10 | Fase 6.2 (ciclo de estados): (a) BORRADOR→VALIDADO es **automático** en cuanto el Responsable en mandato pulsa Validar y sale sin errores (sin botón "Marcar validado" aparte) — pero el servidor siempre revalida por su cuenta contra los datos del store, nunca confía en el resultado que calculó el cliente; (b) editar un mes VALIDADO lo revierte a BORRADOR sin fricción (guardar ya lo permite, no hace falta "desmarcar" antes); (c) PUBLICADO **sí se puede revertir**: el Responsable puede despublicar (PUBLICADO→VALIDADO) para corregir y volver a publicar — no es un estado terminal en esta fase; (d) solo el Responsable en mandato puede marcar VALIDADO, no cualquier residente | Respuestas del autor antes de empezar la 6.2. (a) simplicidad de UI sin sacrificar seguridad, mismo principio que el rol derivado ("nunca un flag que el cliente pueda falsear"); (c) sin vía de corrección, un error publicado por descuido quedaría sin arreglo hasta la Fase 7; (d) el Responsable controla todo el ciclo VALIDADO→PUBLICADO, no solo el último paso |
| V-11 | Fase 7.1 (proyección a Sheets): (a) `publicarCuadrante` proyecta de verdad la pestaña mensual + "Resumen" en el MISMO paso — no hay una acción separada "proyectar"; (b) despublicar (PUBLICADO→VALIDADO) **no toca** ninguna pestaña ya proyectada, se queda con la última proyección hasta la siguiente publicación; (c) "Resumen" agrupa la equidad por **cohorte de ingreso** (mismo criterio que INV-3/V-2), no por nivel actual como hacía "Resumen Anual" del .xlsm; (d) alcance de esta entrega: pestaña mensual + Resumen; Contaje Trimestral queda para una Fase 7.2 aparte | Respuestas del autor antes de empezar la 7.1 (sábado, sin acceso aún a la cuenta de Google del servicio — "adelantemos lo que podamos"). (a) simplicidad: "publicar" pasa a significar publicar de verdad, un solo concepto; `rebuildSheet` (server/src/sheets-store.js, ya implementado en Fase 2 pero sin invocador hasta ahora) es idempotente/autorreparable por shadow-swap, así que reintentar "Publicar" basta ante un fallo parcial — y `router.js` proyecta ANTES de escribir el estado PUBLICADO, para que un fallo de la API de Sheets deje el cuadrante intacto en VALIDADO en vez de un PUBLICADO fantasma con el Sheet a medias; (b) evita que el Sheet "parpadee" mientras alguien lo mira, coherente con que despublicar es para corregir y volver a publicar enseguida; (c) consistencia con el validador real — un umbral "Resumen" agrupado por nivel actual mostraría una equidad distinta de la que INV-3 exige de verdad; (d) menos fórmulas sin verificar en vivo antes del lunes (cuando llega el acceso), mismo patrón de troceo que 4.1/4.2 y 6.1/6.2. **Simplificación NO preguntada, a validar con el autor:** "Resumen" acumula TODOS los meses publicados desde siempre (no por año académico como el .xlsm, que se recreaba cada año) — el `.xlsm` legado no tenía en realidad pestañas por mes (una sola hoja "Cuadrante Anual" con 12 bloques apilados; "1 pestaña/mes" es arquitectura nueva, no una copia), así que no había un precedente literal que replicar. La equidad de INV-3 es por año de residencia individual, no por año académico ni acumulada de por vida — esta hoja es una lectura aproximada, no sustituye al validador |
| V-12 | INV-1: cubrir un día con **1 sola persona** (Mayor solo o Pequeño solo) deja de ser error duro y pasa a **aviso** — no bloquea Validar/Publicar. 0 personas ese día sigue siendo error duro (nunca admisible); cualquier composición con 2+ personas que no sea 1 Mayor+1 Pequeño (o el 2×R2 de INV-9) sigue siendo error | Petición del autor al intentar la verificación en vivo de la Fase 7.1: hay guardias reales cubiertas por 1 sola persona (comida de Navidad, despedida de R4, sobrecarga puntual con rotantes externos u otra circunstancia) — "siempre se debe cubrir pero lo dicho". La versión anterior de INV-1 bloqueaba estos días como si fueran un error de composición, cuando la normativa real los admite |
| V-13 | Fase 7.2 / P-8 (equidad trimestral): (a) el cierre trimestral mide **solo el eje `total`**, no los seis del cierre anual; (b) su severidad es **aviso**, no error — no bloquea Validar ni Publicar; (c) los cierres se **conectan de verdad** al flujo: `marcarValidado` (servidor, autoridad) y `Calendar.jsx` (para que quien valida vea la misma razón) comprueban ahora el trimestral Y el anual; (d) un residente con **menos de medio trimestre** disponible no entra en la comparación; (e) el eje `puentesLibres` de INV-3 sigue **sin comprobarse de verdad** (no hay tabla de festivos/puentes: mismo bloqueo que INV-12) — *(e) queda superado por la fase 3 de V-17: desde entonces el eje se deriva de la tabla `festivos` sobre el año de residencia entero; se conserva escrito porque explica por qué el eje estuvo comparando ceros* | Respuestas del autor antes de empezar. (a) y (b) son lectura literal de la frase de p.2 (ver §5.1): «diferencia de número de guardias» es un solo eje, y la propia frase prevé compensar el exceso «en los meses siguientes», que es la definición de aviso en el criterio V-4 — extender los seis ejes al trimestre sería inventar autoridad que la normativa no da. (c) es lo que convierte P-8 en comportamiento: hasta ahora `validateResidencyYearClose` (INV-3 anual) estaba implementado y probado desde la Fase 3 pero **no lo invocaba nadie**, ni el router ni ninguna pantalla, así que la equidad de cierre no se comprobaba nunca en producción. (d) normalizar `total/disponibilidad` con una fracción diminuta amplifica: media guardia en dos semanas disponibles se convertiría en un "13" y avisaría en falso cada trimestre; quien esté de baja más de medio trimestre sigue cubierto por el cierre anual, cuya ventana promedia. (e) documentado explícitamente para que un eje que compara ceros no se lea como un eje verificado |
| V-14 | **La equidad nunca bloquea, ni el reparto mensual.** Todas las violaciones de equidad son `aviso`, jamás `error`: INV-3 en sus dos cierres (anual y trimestral) e INV-8 en su diferencia de 3.º puestos acumulados. **INV-2** (4-6 guardias/mes) baja también a `aviso` en las dos direcciones, porque p.2 degrada esas cifras con todas las letras («Estas cifras son orientativas. El criterio obligatorio será [la equidad]»); no entra en `EQUITY_INVARIANTS` a propósito: un aviso de recuento mensual salta casi todos los meses y una confirmación que salta siempre deja de ser una decisión. **Bajan también a `aviso` INV-6, INV-7 e INV-9**, por el mismo criterio extendido: su causa no vive en el cuadrante del mes (ausencias ya concedidas, huecos de viernes/sábado que pueden no existir, una Excepcion que hay que ir a registrar aparte), así que bloquear por ellas es dejar un mes sin validar por algo que nadie puede corregir DENTRO del propio cuadrante que se está validando. **Lo único que sigue bloqueando es lo imposible y lo ilegal**: un día que nadie cubre o una composición de 2+ personas que no puede ser (INV-1), una guardia sobre una baja médica (INV-5) y un R1 asignado en junio-agosto (INV-11). Validar un cuadrante que incumple la equidad **sí exige confirmación explícita** en la UI («Validar de todas formas»), mostrada junto a los avisos y después de poder leerlos. Los invariantes de equidad están enumerados en `cuadrante.js` (`EQUITY_INVARIANTS`), no reconocidos por el texto del mensaje | Petición del autor (2026-07-26), con el criterio rector del proyecto por delante: la app tiene que pasar de generación en generación sin administrador, y una regla de equidad que bloquea deja al servicio **sin cuadrante** cuando nadie sabe desbloquearla — el coste de bloquear es mayor que el del aviso. Además la normativa está de su lado: p.2 prevé compensar el exceso «en los meses siguientes hasta equilibrar el cómputo dentro del año de residencia», así que un desequilibrio es por definición corregible y encaja en el criterio V-4 (aviso = advisory/compensable). La confirmación explícita evita el otro extremo: que un aviso que nadie mira se convierta en equidad que nadie vigila |
| V-15 | **P-7 resuelto: un solo cuadrante mensual.** Los «2 grupos» de la normativa (R1-R2 / R3-R4) y su «cuadrante provisional» por grupo describen la **composición** de cada guardia (siempre un Pequeño con un Mayor, que es INV-1) y cómo se organiza cada grupo para repartirse los días — no dos tablas ni dos pestañas. El modelo de datos, el ciclo BORRADOR→VALIDADO→PUBLICADO (V-9/V-10) y la proyección a Sheets (V-11) siguen operando sobre **un** cuadrante mensual, sin cambios | Lectura del autor (2026-07-26), que es quien conoce el uso real: «se describen dos cuadrantes R1 y R2 —— R3 y R4; pero sí que se puede poner todo en el mismo cuadrante mensual […] eso afecta únicamente a cómo se ponen las guardias, no a cómo se expresan los datos en las tablas de excel». Cierra la propuesta más profunda de §8 sin tocar una línea de código: lo que la normativa exige ya lo cumple INV-1 |
| V-16 | **Sin Responsable designado, el ciclo no se bloquea.** Si no hay mandato vigente a día de hoy, cualquier residente **Mayor** (R3/R4, derivado de fechas como todo lo demás) puede validar, publicar y despublicar, y la pantalla lo dice en vez de callárselo («no hay Responsable del contaje designado para este periodo»). Con mandato vigente sigue siendo exclusivo del titular (V-9c, sin cambios). NO se crea ningún mandato ficticio: INV-14 queda intacto, esto es una regla de permiso, no un registro. El permiso se relee del store en CADA llamada (`requireCicloPermiso`), no del `rol` firmado dentro del token, que puede ser de hace horas. Además, `estadoResponsable` devuelve también el periodo SIGUIENTE, y tanto Inicio como la pantalla de Responsable invitan a ofrecerse voluntario para el mandato que aún no está decidido | Petición del autor (2026-07-27) al descubrir que el mandato de 2026 no lo tiene nadie en producción y que, por tanto, **nadie podía validar ni publicar**: el ciclo entero de las Fases 6 y 7 estaba muerto por una fila que falta en una tabla. El criterio rector manda otra vez (≥10 años sin administrador): en algún enero de los próximos diez años nadie lanzará el sorteo, y un cuadrante que no se puede publicar es peor que uno publicado por el R4 que estaba delante. Se limita a Mayores porque la normativa hace recaer el contaje en un R3/R4, nunca en un Pequeño. La invitación al voluntariado del periodo siguiente existe para que la ausencia de mandato se corrija sola: el aviso solo se le muestra a quien puede actuar (elegible, sin decidir, sin ofrecerse aún) y desaparece en cuanto se ofrece |
| V-17 | **Festivos como tabla, e INV-12 en vigor.** (a) Los festivos son una tabla de datos (`fecha`, `nombre`, `ambito`, `activo`), cargada en lote y corregible reinsertando `activo=false`, nunca derivada ni pedida a la IA (S-4); se guardan **fecha a fecha** e incluyen los **locales de Alicante**, porque son festivos reales que cambian cada año y sin ellos INV-12 avisaría en falso sobre GF correctas. (b) Los **puentes se derivan** de esa lista (`bridgesOfMonth`), nunca se escriben. (c) INV-12 es **aviso** siempre. (d) Cuando falta el calendario, INV-12 avisa **solo si hay GF escritas** en el mes, no en todo mes sin festivos: febrero no tiene ninguno en España, así que avisar sin más sería un aviso falso cada febrero — se avisa cuando la falta impide comprobar algo que alguien ya escribió. (e) Cargar y anular festivos usa el permiso del ciclo (V-16), porque es dato compartido de todo el servicio; leerlos está abierto a cualquier sesión | Decisión del autor (2026-07-27) sobre las tres fases y el ámbito. (d) matiza su respuesta original («si falta el año, que avise»): la intención era no callar cuando no se puede comprobar, y eso se cumple sin gritar cada febrero. El orden de las fases es de dependencia pura: sin la tabla, INV-12 no puede existir; y el eje `puentesLibres` de INV-3 (fase 3) necesita además la lista de DOS años naturales, porque el año de residencia cruza mayo. **Las tres fases están implementadas** (fase 3 en §7, contrato C-3 de CLAUDE.md) |
| V-18 | **El tercer puesto entra en vigor: tabla `voluntarios3P`, permanencia de 4 meses y las cuatro reglas en aviso.** (a) INV-8a (3P a quien no consta voluntario) e INV-8b (repetir día de semana) **bajan de `error` a `aviso`**: con 8c y 8d ya en aviso, ninguna de las cuatro reglas del 3.º puesto bloquea. (b) El voluntariado es **autoservicio y sin periodo**: cada residente se apunta cuando quiere —nadie apunta a nadie, ni el Responsable— y su ciclo L-D **empieza el día en que se apunta**, por el día de la semana que prefiera. (c) A cambio, al apuntarse asume un **compromiso de permanencia de 4 meses** que **acepta explícitamente** (`compromisoAceptado`, registrado) y que el servidor hace cumplir: hasta cumplirlo, `retirarVoluntariado3P` se rechaza diciendo la fecha. (d) La tabla `voluntarios3P` NO lleva `periodoInicio` (a diferencia de `voluntariosResponsable`): lleva `desde`, la fecha de alta real, porque es lo que acota el ciclo de 8b y lo que fija el compromiso. (e) `thirdPostHistoryStart` decide el histórico que hay que leer: el `desde` de cada voluntario para 8b, y la ventana del año de residencia para 8c, la más antigua de las dos | Respuestas del autor (2026-08-02) al preguntarle por el modelo antes de escribirlo. Sobre (b)/(c), literal: «cada residente se apunta si quiere y cuando quiere y puede empezar el día de la semana que quiera con ese criterio de no repetir días de la semana hasta completar el ciclo de 7 días, lo que pasa es que tiene que ser continuo durante todo el año, por eso cuando te apuntas debes tener al menos un periodo de permanencia de varios meses (al menos 4 meses, que debería salir como un disclaimer que tienen que aceptar cuando te apuntas)» — «flexible a medias». La permanencia no es burocracia: sin ella la rotación L-D queda a medias y 8c se queda sin con quién comparar. Sobre (a), el criterio de V-14 llevado a su conclusión: la causa de un 8a o un 8b no siempre vive en el cuadrante del mes (alguien se apuntó después; el ciclo arrastra de meses que ya nadie va a reabrir), así que bloquear por ellas deja al servicio sin cuadrante por la regla más blanda que tiene la normativa. INV-8 sigue en `EQUITY_INVARIANTS`, así que la confirmación explícita de la UI lo cubre. Esto es además lo que **cablea** INV-8: estaba implementado y probado desde la Fase 3 sin que lo invocara nadie, y no se podía conectar antes porque sin tabla de voluntarios marcaba TODO 3P como no-voluntario |
| V-19 | **Las ausencias las manda `bloqueos`, y el Responsable puede registrar la de otro.** (a) Un **lector único** en el dominio (`v2/domain/absences.js`): los criterios de INV-5, INV-2, INV-6, INV-7 y el descuento de disponibilidad de INV-3 estaban escritos a mano en cinco `filter` distintos. No se colapsan —cada invariante mira motivos distintos a propósito—: se les pone nombre (`BLOQUEA_ASIGNACION`, `EXIME_DEL_MINIMO`, `AUSENCIA_SIMULTANEA`, `DESCUENTA_DISPONIBILIDAD`) y el filtro pasa a ser uno. Lo que sí unifica es `activo`: una fila cancelada la descarta el dominio, ya no el invocador. (b) `crearBloqueo` acepta un `residenteId` **opcional**, y solo con el permiso del ciclo (V-16): sin él sigue escribiendo `session.sub`. (c) `cancelarBloqueo` es simétrica, por el mismo permiso. (d) NO se guarda quién registró la ausencia ajena | (a) el `activo` vivía únicamente en `bloqueosInRange` del router, así que bastaba con que un invocador nuevo no pasara por ahí para que un bloqueo **cancelado** volviera a bloquear asignaciones, y ningún test lo habría notado. (b) es la acción que faltaba: la tabla `bloqueos` es la que leen los invariantes, y ante una baja que el residente no ha declarado —y que precisamente por estar de baja puede no poder declarar— el único gesto posible era pintar una «B» en la rejilla, que es un **código de asignación** y no lo lee NINGÚN invariante: INV-5 seguía dejando asignarle guardias. (c) sin ella una baja puesta al residente equivocado sería irreversible: la tabla es append-only y el afectado no creó la fila, así que tampoco podía cancelarla él, y le habría bloqueado las asignaciones para siempre. (d) evaluado y descartado: `bloqueos` ya tiene filas en producción y `rowsToRecords` mapea por POSICIÓN, así que una columna nueva deja en el Sheet una columna sin nombre en la cabecera que solo se repara a mano — mal negocio para una app que tiene que sobrevivir sin administrador |
| V-20 | **INV-10 e INV-13 modelados y en vigor.** (a) **Eventos** (INV-10): tabla `eventos` (`tipo` NAVIDAD\|DESPEDIDA, `fecha`, `voluntarios`, `designados`, `sorteoId`, `activo`), dato de entrada como los festivos. `designados` se **almacena**, no se deriva de quién tuviera guardia ese día. El «a sorteo» de la normativa se hace de verdad y **reproducible**, reutilizando la tabla `sorteos` y el sorteo de INV-14 (V-7a); `sorteoDocumentado` se deriva de que exista esa fila. Con ≥2 voluntarios se sortea solo entre ellos (criterio V-7b). `buildMonthContext` empareja Navidad y despedida por **año académico**. (b) **Imaginaria** (INV-13): es una **herramienta, no un validador**. `imaginaria.js` deriva la cola del grupo —orden por cobertura más antigua, quien nunca cubrió primero, desempate por id— y aparta a quien tenga guardia la víspera, el día siguiente o esa misma noche; un R1 nunca entra en la lista de Pequeños. La cola NO se almacena: se registra cada cobertura (`imaginaria`) y el orden se deriva. Registrar **no exige** seguir el orden. (c) INV-13 baja de `error` a **aviso** | (a) `designados` almacenado es respuesta directa del autor: validar la despedida de mayo necesita saber quién cubrió la Navidad del diciembre anterior, y guardarlo evita que junio lea las asignaciones de diciembre (contrato tipo C-2 que nadie tendría que acordarse de cumplir). Contrapartida aceptada: si alguien cambia la guardia del día de Navidad después, esa lista no se entera. El sorteo reproducible en vez de un booleano «hubo sorteo» sigue el criterio de V-7a: un flag autodeclarado no prueba nada, y la normativa pide sorteo justamente para que el reparto no sea a dedo. (b) respuesta del autor: lo que hace falta a las ocho de la mañana con una baja encima es saber a quién llamar, no un aviso a fin de mes. Validar a posteriori que cubrió «el que tocaba» saltaría a menudo por motivos legítimos que la app no ve (nadie cogía el teléfono, se cambió con otro), así que `validateMonth` no llama a `imaginaria.js`. El orden, la exclusión del R1 y la exclusión por proximidad son práctica real del servicio (P-11), NO normativa: declarados como extensión en §5.1. (c) coherente con V-14 y, siendo herramienta, la severidad casi no llega a usarse |
| V-21 | **INV-1: una guardia asignada a quien no es asignable en esa fecha se nombra, y avisa en vez de bloquear.** (a) Se emite una violación **por asignación** que dice la causa —residencia terminada (con su fecha), residencia sin empezar (con su fecha), o id que no está en `residentes`— y que trae `residenteId`. (b) El día que solo cubren no asignables deja de reusar el mensaje del día vacío y dice «sin nadie asignable», nombrando a quién. (c) Las dos son **aviso**. (d) 0 asignaciones ese día sigue siendo **error** duro, y la presencia de un no asignable **no** degrada el error de una composición ≥2 incorrecta (2×Mayor sigue bloqueando) | Los dos casos estaban mal diagnosticados, no solo mal clasificados: un día cubierto SOLO por un `FINALIZADO` producía el objeto **byte a byte idéntico** al de un día vacío («Día … sin cubrir, mayores=0, pequeños=0»), sin `residenteId` y por tanto sin nada que `client/lib/violations.js` pudiera traducir a un nombre — un error duro que miente y no da una sola pista de la causa. Y el caso inverso, 1 válido + N no asignables, hacía lo contrario: avisaba de infra-cobertura y no decía **nada** de la asignación inválida, que llegaba a VALIDADO, a PUBLICADO y a la pestaña proyectada. El disparador no es exótico: el R4 que termina a mediados de mayo mientras se dibuja junio/julio, o una `fechaFin` mal teclada. Sobre la severidad (decisión del autor, 2026-08-02): `aviso`, porque la causa vive en las fechas del residente y **no existe `editarResidente`** (§7, nota [a]) — bloquear dejaría al servicio sin cuadrante por algo que nadie puede corregir DENTRO de la herramienta, que es el criterio de V-12/V-14/V-16. Contrapartida asumida y dicha en voz alta: un mes puede publicarse con un día que de hecho no cubre nadie, y lo único que lo evita es que el aviso se lea |
| V-22 | **Validación de entrada en las escrituras, y una ausencia con fecha ilegible se nombra en vez de juzgarse.** (a) Un solo `validRango` valida los rangos de fecha de entrada con `parseISO` de verdad —lo comparten `crearBloqueo`, `listBloqueosRango`, `listAsignacionesRango` y `listFestivosRango`/`listEventosRango`—, en lugar del `desde > hasta` lexicográfico que tenía cada una. (b) `guardarAsignaciones` gana listas blancas de `codigo` (los siete de la rejilla más el `""` del borrado) y de `origen` (CEDIDA\|COMPRADA), validadas ANTES de escribir el lote. (c) Una fila de `bloqueos` con fecha ilegible **ya escrita** se aparta del contexto de los invariantes y se reporta con un `error` INV-5 que da motivo, las dos fechas, el `residenteId` y el `id` para cancelarla; **sigue apareciendo** en `misBloqueos`/`listBloqueos`, en todos los meses, porque esconderla la volvería incancelable | (a) el `>` lexicográfico colaba cualquier cosa cuyo primer carácter ordenase por debajo: `"30/02/2027"`, `"2027-13-45"`, `"9999"` y un objeto entraron con `{ok:true}` en la sonda del 2026-08-02. En una tabla append-only sobre un Sheet que se edita a mano eso se queda para siempre. (b) las erratas de `codigo`/`origen` son MUDAS: una `"g"` minúscula no la reconoce ni `GUARDIA` —INV-1 da el día por descubierto— ni `tally`, y `origen` se evalúa por TRUTHINESS (`tally.js:15`), así que cualquier errata saca la guardia de los seis ejes de INV-3 y de los totales de la pestaña publicada. (c) el diagnóstico anterior era el peor de los mundos, y no por una excepción —`validateMonth` solo compara cadenas y no lanza nunca por esto— sino porque **el veredicto de INV-5 salía a suerte**: medido, con `hasta="no-es-fecha"` emitía su violación de puro accidente (`"2"` ordena antes que `"n"`), y con `desde="30/02/2027"` emitía CERO, o sea la baja médica dejaba de proteger en silencio; y los cierres de equidad, que sí hacen aritmética, tumbaban `marcarValidado` con un «Fecha ISO inválida» que no decía de qué tabla, de quién ni cuál. Es `error` y no `aviso` —decisión del autor— porque no es una regla nueva que bloquea: es INV-5 diciendo que no puede comprobarse, ya bloqueaba antes con un mensaje ciego, y **sí tiene salida dentro de la herramienta**, que es lo que V-14 exige: cancelar la fila y recrearla, y desde V-19 eso vale también para la ausencia de otro |
| V-23 | **La rejilla se agrupa por el nivel del MES que dibuja, no por el de hoy, y el no asignable con algo escrito tiene fila propia solo-para-borrar.** (a) `client/screens/Calendar.jsx` deriva el nivel con la misma regla que la pestaña publicada (`v2/domain/projection.js`, `activeResidents`): sale quien esté activo algún día del mes, etiquetado por su nivel en su **primer día activo** dentro de él. (b) Quien no puede hacer guardias ningún día del mes **pero tiene algo escrito en la rejilla** sale en una fila aparte al final, marcada con la causa y su fecha frontera; sus celdas **solo borran**, nunca asignan, y la fila desaparece al vaciarla. (c) Es UI: no cambia ningún invariante ni ninguna severidad | (a) el ancla era `todayISO()`, así que la rejilla y el validador discrepaban sobre el mismo dato: un mes que cruza un aniversario se agrupaba por el nivel de HOY mientras `validateMonth` lo juzga día a día (ya congelado por el otro lado en el test «INV-1: nivel día a día»), y —peor y no advertido hasta ahora— un FINALIZADO desaparecía de **todos** los meses, incluidos los **pasados** en los que sí hizo guardias, que quedaban invisibles y no editables. De paso seguía ofreciéndose como asignable en meses futuros en los que ya no lo es, que es el disparador del punto 4 de la auditoría del 2026-07-27. Se reusa el ancla de la proyección y no `dias[0]` porque `dias[0]` escondería al R1 que entra a mitad de mayo (el día 1 todavía es PENDIENTE), y porque así rejilla y pestaña publicada enseñan por fin las mismas filas con las mismas etiquetas. (b) sin esa fila, arreglar (a) abre un agujero peor que el que cierra: la celda que INV-1 señala (el aviso de V-21) sería justo la única que no se puede ver ni tocar, y la tabla es append-only. **Solo-borrar** y no «solo lectura» porque asignar es exactamente lo que no debe poder hacerse ahí y borrar es exactamente lo que hay que poder hacer; y la fila solo existe si hay algo que limpiar, así que no es un recordatorio permanente sino la lista de lo que queda por arreglar, que se autodestruye. Cubre de sobra el aviso de V-21: aquel solo se emite sobre códigos de guardia, esta fila sale con cualquier código escrito |
| V-24 | **Los periodos formativos se derivan en UN solo sitio, se pueden editar y las fechas de un residente se pueden corregir.** (a) Fase 1: Se reencaminan las SIETE copias del `residente.fechaFin || addYears(+4)` que había repartidas por el dominio — `validate.js`, `thirdpost.js`, `responsible.js`, `projection.js`, `accumulate.js` y las **dos** de `equity.js` (`closingWindowThisMonth` y el rango de presencia del cierre trimestral, que el informe de auditoría no listaba). (b) Fase 2: un lector único `allResidentes` en el router monta `residente.periodos` desde la tabla, traduciendo `{anio,fechaInicio,fechaFin}` → `{year,start,end}`; hidratar en unas acciones y no en otras sería peor que no hidratar. Un residente sin las 4 filas no recibe la clave y sigue derivando de sus fechas. (c) Fase 3: acciones `editarResidente` (corrige `fechaInicio`/`fechaFin`, nunca el email — es la llave del login), `guardarPeriodos` (las 4 filas de golpe, validadas con `validateTrainingPeriods`, que no invocaba NADIE) y `restaurarPeriodos` (vuelve a los derivados: sin ella una edición mala sería irreversible en una tabla append-only, que es el bloqueo sin salida que V-16 tuvo que arreglar de urgencia). Las tres exigen el permiso del ciclo (V-16): el retraso de una promoción lo decide tutoría, no el propio residente, y NO se deriva de `bloqueos` —una baja de dos semanas no retrasa nada, y adivinarlo cambiaría el nivel de alguien sin que nadie lo haya decidido | Tres de esas copias —`projection`, `accumulate` y `closingWindowThisMonth`— **no miraban `residente.periodos`**, así que con la tabla `periodos` rellena el mismo residente habría tenido dos niveles distintos dentro de una sola llamada a `marcarValidado`. No es teórico, está medido: para el MISMO residente y el MISMO día, `projection.js` daba R2 mientras `validate.js` daba R1. Y la consecuencia grave no es cosmética — un R1 alargado por baja que se deriva como R2 pasa a ser asignable en junio-agosto **sin que INV-11 lo vea**, y INV-11 es uno de los tres invariantes que bloquean. Por eso esta fase va ANTES de poder escribir en la tabla, y no después. `closingWindowThisMonth` además ignoraba las dos vías por las que un año puede no acabar en el aniversario nominal: comprobado que con `fechaFin` nominal el resultado es idéntico mes a mes (o sea, para todos los residentes de hoy no cambia nada) y que donde cambia es donde estaba mal — un R4 que deja la residencia el 15 de marzo cerraba su año en MAYO, dos meses después de irse. Ningún test cubría esto: antes de la unificación `periodos:` solo aparecía en `residents.test.js`, así que los consumidores nunca se probaban con periodos editados |
| V-25 | **Fase 7.2 completa: Contaje Trimestral existe, y la ventana de las hojas agregadas es el CURSO académico.** (a) Nueva hoja `Contaje Trimestral YYYY-YY`: total por residente y trimestre del contaje (T1 jun-ago … T4 mar-may, agrupando **por mes** vía `trimesterOf`) más un bloque de diferencia máx-mín por cohorte. Muestra **solo el total**, que es el único eje del cierre trimestral (V-13/P-8), no la carga anual por nivel del .xlsm. (b) `Resumen` y `Contaje Trimestral` se acotan al **curso académico** y lo llevan en el NOMBRE, así que la hoja del curso pasado no se sobreescribe. (c) La columna «Equidad (dif. ≤ 1)» pasa a «Reparto del curso (orientativo)» y su veredicto deja de decir OK/REVISAR. (d) Las dos hojas escriben en la propia hoja una nota con sus tres límites. (e) Las tres constructoras devuelven matriz rectangular | (b)+(c) el cartel afirmaba ser el umbral de INV-3 y era falso en las DOS direcciones, medido: la ventana de INV-3 anual es el año de residencia de cada uno, que arranca en su aniversario y no en junio, así que cuatro cierres anuales con dif=1 —los cuatro OK— acumulan una dif de por vida de 4 y la hoja habría puesto REVISAR sin que INV-3 avisara nunca; y al revés, los dos cierres NORMALIZAN por disponibilidad, de modo que con una baja de 6 meses INV-3 calla mientras un MAXIFS/MINIFS sobre totales brutos marca una diferencia de 25. Tras V-14, este cartel es una de las pocas señales de equidad visibles fuera de la app, así que decir de menos es mejor que decir de más. El curso, además, acota la cadena SUMIF a ≤12 términos por celda: de por vida crecía un término por mes publicado y por celda, ~10.800 SUMIF de columna completa por recálculo a los diez años. (a) el .xlsm agrupaba por nivel y por rangos fijos de fila, que es el «bug de trimestres posicionales» que ADR-001 manda eliminar (al insertar un residente se desalineaba en silencio) y que además parte T3, que cruza el año natural. (d) el Sheet es el entregable y lo lee gente que no abre el repo: sin la nota, «Dif. máx-mín» se lee como el «dif ≤ 1» de la normativa. (e) `setValues` exige matriz rectangular y las filas de nota y separación son lo primero que lo rompe; ningún test lo habría visto, porque el `ss` de los fakes es un array de arrays sin límites — la misma ceguera del fallo de la rejilla de 26 columnas |
| V-26 | **El cliente reintenta los fallos de transporte de las acciones idempotentes.** `callBackend` reintenta 2 veces (400 ms y 1200 ms) cuando la respuesta no es `ok` o hay excepción de red, y **solo** para las acciones de una lista blanca explícita: lecturas, `getNonce`, `login` y `validar`. Las escrituras NO se reintentan. Un `{ok:false}` del servidor tampoco. Y el mensaje deja de ser «HTTP 404» pelado | El `/exec` de Apps Script no contesta directo: responde **302 a un enlace temporal de un solo uso** que el navegador sigue, y cuando ese segundo salto falla —enlace consumido, caducado, o Google estrangulando la tasa— llega **HTML de Google con un 404**. Reproducido en producción el 2026-08-05: dos `login` idénticos y seguidos dieron 404 y 200, y midiendo seis peticiones seguidas las dos primeras fueron bien y las cuatro siguientes ni respondieron. El residente veía «HTTP 404» al entrar en la app, sin nada que hacer salvo insistir a ciegas y sin que nada le dijera que era transitorio — inaceptable para algo que debe funcionar sin administrador. El servidor ya tenía esta red para el mismo tipo de inestabilidad (`fetchTokeninfo_` reintenta 3 veces con backoff); el cliente no la tenía. La lista blanca es explícita y NO se deriva del nombre a propósito: olvidar una lectura cuesta un reintento perdido, pero colar una escritura costaría una fila duplicada en una tabla append-only que nadie borra. `login` entra aunque consuma el nonce, porque en el caso mayoritario —el POST no llegó a ejecutarse— el reintento resuelve el login, y en el minoritario el usuario ve «nonce reusado» en vez de «HTTP 404», que no es peor |
| V-27 | **El sesgo de ausencias en `puentesLibres` se corrige con DOS cambios juntos, no uno.** (a) Un puente cubierto por BAJA, VACACIONES o ROTACION deja de contar como «libre»: se excluye del recuento (`absences.js:AUSENTE_EN_PUENTE`, los tres motivos). (b) `puentesLibres` entra en `PROPORCIONAL` y se divide por la `f` de disponibilidad —solo BAJA, nota [a], sin tocar V-8— igual que los demás cinco ejes | Preguntado directamente por las dos correcciones que spec.md dejaba anotadas como alternativas: excluir SOLO reduce el numerador de quien se ausenta, sin nada con qué compararlo; normalizar SOLO divide un numerador que sigue inflado por la propia ausencia, empeorándolo. Aplicadas juntas: la exclusión arregla primero el numerador (ya no cuenta como "libre" lo que no fue una decisión del reparto), y la normalización compara lo que queda a una tasa común, igual que ya hace con `total`. Ninguna de las dos tiene respaldo normativo directo — es criterio del autor, igual que el resto de `PROPORCIONAL` |
| V-28 | **INV-8a juzga "¿era voluntario ESE día?", no "¿lo es ahora?".** `validateThirdPost` gana `periodosVoluntario3P` opcional (todos los periodos de voluntariado, activos e históricos); sin él se conserva el criterio anterior. `allThirdPostPeriods` (router) reconstruye los periodos con `readRecords` crudo —`readLatest` colapsa a una fila por residente y pierde el periodo de en medio de quien se apuntó, se retiró y volvió a apuntarse— fundiendo cada alta con su baja por `residenteId+desde`. 8b/8c no se tocan: siguen juzgando sobre la lista de voluntarios de HOY, que es lo correcto para el ciclo vigente y el cierre de equidad | Cerraba la laguna anotada en §7 desde V-18: con la lista de HOY, a quien se retiró se le avisaba en falso por 3P legítimos de antes de retirarse, y a quien se apuntó hace poco se le dejaba de avisar por 3P de antes de existir su alta. Sigue aviso, nunca bloquea (V-18) — esto corrige EL DIAGNÓSTICO, no la severidad. Al escribir el test de punta a punta apareció una fixture preexistente en `router-cuadrante.test.js` que fechaba un 3P dos días ANTES del alta del voluntario y pasaba porque el criterio viejo no miraba fechas — corregida a una fecha posterior al alta, que es lo único que puede pasar en la realidad |
| V-29 | **Excepcion (INV-9) se puede registrar desde la app.** Tabla `excepciones` (append-only, `activo` para anular por reinserción, igual que `festivos`/`eventos`): `tipo` (hoy solo `"2xR2"`, lista blanca en el router), `desde`/`hasta`, `justificacion` obligatoria, `registradaPor`/`fecha` automáticos. Acciones `listExcepciones` (abierta, la necesitan el validador y el generador), `crearExcepcion`/`anularExcepcion` (permiso del ciclo, V-16). `buildMonthContext` gana el parámetro `excepciones` (antes ni existía: `validateMonth` ya sabía leer `ctx.excepciones` desde que se implementó INV-9, pero nada se lo pasaba) y `buildCuadranteCtx`/`monthSnapshot` la cablean hasta `marcarValidado`. Pantalla: tercera tarjeta en `DatosServicio.jsx`, que pasa a llamarse "Datos del servicio" en vez de "Festivos y eventos". `Calendar.jsx` también la pide en su `validar()` local, para que el aviso que enseña en pantalla coincida con el que dará el servidor al validar de verdad — mismo criterio que ya tenía con `festivos` | Cerraba una laguna real: `validateMonth` llevaba probado desde que existe INV-9 que un 2×R2 documentado deja de avisar, pero la fila que lo documenta no tenía dónde nacer — ni tabla, ni acción, ni pantalla. Sin esto, INV-9 avisaba siempre a partir de diciembre aunque la rotación de mayores fuera real y estuviera justificada, y quien validara no tenía forma de decírselo a la herramienta. **`Generator.jsx` queda deliberadamente sin tocar**: esa pantalla construye su propio `ctx` con `buildMonthContext` y hoy no le pasa `excepciones` (tampoco le pasa `eventos`, laguna previa ya anotada en §7), así que un 2×R2 con excepción real seguiría marcándose ahí aunque `Calendar.jsx` y el servidor ya no avisen — no se cerró para no pisar la reescritura async de `Generator.jsx` en curso en otra rama (Fase 6.1/V-29 del generador, numeración de Quique, sin relación con esta) |
| V-30 | **`origen` CEDIDA/COMPRADA (INV-4) se marca desde `Calendar.jsx`, con mantener presionado.** El servidor ya validaba y guardaba `origen` desde V-22, y `tally`/`projection.js` ya lo excluían correctamente del cómputo (INV-4) — la única pieza que faltaba era la escritura desde algún cliente. Gesto elegido: mantener presionada ~500ms una celda con G/GF/GP/3P abre un `<select>` inline (normal/cedida/comprada); un click normal sigue ciclando el código exactamente igual que antes, sin ningún retraso. **Descartado a propósito el doble-click**: el DOM dispara el `click` completo (con su efecto de ciclar código) ANTES del `dblclick`, así que habría exigido retrasar ~200-250ms CADA click simple de la grilla para poder distinguirlo de la primera mitad de un doble-click — inaceptable en la interacción más repetida de la app. Cambiar el CÓDIGO de una celda (ciclar o borrar) limpia `origen`: una guardia nueva no hereda la marca de la que hubiera antes. La celda muestra el código con un `*` (p.ej. "G*"), mismo signo que ya usa `projection.js` en la pestaña de Sheets (V-11) para que las fórmulas la excluyan — mismo lenguaje visual en las dos puntas | Cerraba el último tramo de una cadena que ya estaba completa en el resto: `ASIG_ORIGENES` en el router (V-22), la exclusión en `tally.js` y el marcado "*" en `projection.js` (V-11) llevaban meses en vigor sin que ningún cliente pudiera producir el dato que consumían. El diseño del gesto se discutió explícitamente antes de tocar la pantalla más usada en producción: doble-click se descartó por el coste medido de latencia que imponía al click simple, no por preferencia estética |
| V-31 | **Aplicar el generador REEMPLAZA el mes, y una respuesta que no describe el mes pedido se rechaza entera.** (a) `client/lib/apply-month.js:monthReplacementPlan` monta el lote de `guardarAsignaciones`: la propuesta **más** una fila de borrado (`codigo:""`) por cada guardia previa del mes que la propuesta no reemplaza por clave `fecha\|residenteId`. (b) Las marcas **V/R/B se conservan**: el generador no las propone (su formato de salida es G\|GF\|GP\|3P), ningún invariante las lee (`tally.js` solo computa G/GF/GP) y la ausencia de verdad es una fila de `bloqueos` (V-19) — borrarlas sería destruir datos sobre los que no tiene opinión. (c) La pantalla dice cuántas guardias se pierden **antes** de pulsar, y el botón pasa a «Reemplazar el cuadrante de \<mes\>». (d) Se rechaza la respuesta entera —sin veredicto y sin botón— si trae **fechas que no son del mes** o **`residenteId` que no es de nadie**. Esto NO contradice V-14: no es un invariante subiendo a `error`, es un defecto de la respuesta de una máquina, y rechazarla no deja al servicio sin cuadrante porque la rejilla de `Calendar.jsx` sigue estando ahí | `guardarAsignaciones` es append-only con clave `fecha\|residenteId`: mandar solo las filas nuevas **añade**, nunca sustituye. El generador tampoco leía lo que el mes ya tenía, así que validaba su propuesta contra un mes que creía vacío. Consecuencia real y reproducible en producción: generar dos veces con el modelo moviendo a alguien de día dejaba **tres personas** ese día —el Mayor viejo nunca se borraba— justo después de un «✅ Sin violaciones». Igual sobre un mes empezado a mano. Los dos rechazos de (d) cierran los dos huecos por los que la propuesta podía no ser el mes: una fecha de otro mes es **invisible** para `validateMonth` (`byDay`/`dayset` solo tienen días del mes) pero `guardarAsignaciones` la escribiría igual, en el mes vecino y cambiándole el estado; y un id inventado solo produce `aviso` en INV-1 (V-21), así que se aplicaba y dejaba filas que nadie puede ver ni corregir desde ninguna pantalla, en una tabla que no se borra nunca. El id inventado **apareció de verdad** al verificar (a): un `res-carl` por `res-carlos` escribió 31 filas fantasma, y con el reemplazo ya puesto además borró el cuadrante bueno a cambio de nada visible — es (a) lo que vuelve obligatorio (d). Verificado en vivo contra `dev-server.mjs`, agosto-2026: mes lleno con Ana+Elena, «V» de vacaciones puesta a mano a Iván, y una segunda propuesta con Carlos → avisa de que se pierden 31, Ana queda a 0, Carlos a 31 y la «V» de Iván sigue intacta. 13 tests nuevos |
| V-32 | **El ±1 de INV-3 es alcanzable y se queda como restricción; el 4-6 de INV-2 no lo es y por eso sigue siendo `aviso`.** Medido el 2026-08-08 con el juez real (`validateResidencyYearClose`), la plantilla real del `.xlsm` (3 R4 / 4 R3 / 4 R2 / 4 R1) y el calendario laboral de Alicante verificado, sobre la ventana 2026-05-27→2027-05-26. (a) El ±1 en los **seis** ejes se alcanza en todos los escenarios probados —base, baja de 3 y de 6 meses, dos bajas simultáneas, cohortes de 2, plantilla mínima 2/2/2/2— y sin producir un solo `error` de `validateMonth`. (b) El 4-6 mensual de INV-2 es **aritméticamente inalcanzable** en las dos direcciones con esta plantilla. (c) Tres reglas para quien escriba un generador: hay que perseguir el **acumulado del año**, no el mes; hay que comparar `total/f` y no `total`; y un buscador que solo **intercambia** asignaciones no puede arreglar nunca el eje `total`, porque un intercambio conserva el número de guardias de cada uno. **Corrección del 2026-08-11, tras rebasar sobre el V-27 de `main`:** aquel cambio hizo DOS cosas —metió `puentesLibres` en el conjunto normalizado por disponibilidad, y dejó de contar como libre un puente caído dentro de una ausencia— y el banco llevaba su propia copia de las dos reglas, así que quedó persiguiendo un objetivo distinto del que el juez le mide **sin que ningún test fallara**: cada copia era coherente consigo misma. Corregido importando `DIMS`, `PROPORCIONAL` y `residentIsFreeOnBridge` del propio `equity.js`. **La conclusión de (a) no cambia: con el objetivo ya alineado el ±1 se alcanza 3/3 en los seis escenarios, bajas largas incluidas.** Hubo una medición intermedia que daba 0/3 en «baja de 6 meses» y «dos bajas simultáneas» y que llegó a escribirse aquí: era falsa, y la delató que subir el presupuesto de búsqueda ×6 no cambiaba ni el resultado ni el tiempo — el buscador se paraba creyendo haber terminado, que es la firma de un coste desalineado y no de un óptimo difícil. Se midió además que la generación **mes a mes con el pasado inmutable** no llegaba al ±1 con una baja larga (0/3) ni siquiera normalizando, y se atribuyó a límite del generador incremental. **Corregido el 2026-08-13 (V-37): eso era en buena parte la tolerancia del propio juez**, que se encogía al normalizar. Con ella arreglada, el mismo banco llega al ±1 anual con baja en 1 de 2 semillas, y `schedule.js` sobre 12 meses encadenados con una baja de 3 meses cierra el año **sin una sola violación** en las tres semillas probadas. Queda límite del incremental, pero es más pequeño de lo que decía esta fila y hoy se ve en el cierre TRIMESTRAL (2 avisos al año con baja), no en el anual. Lo determinista queda fijado en `test/equidad-factible.test.js` (5 tests, dentro de `npm test`); la búsqueda aleatorizada vive en `test/bench/equidad-factible.mjs`, **fuera** de la suite a propósito, porque tarda segundos y la suite entera tarda uno | La duda era de diseño, no académica: si el ±1 no fuera alcanzable dejaría de ser una restricción que perseguir y habría que degradarlo a objetivo aproximado, y el diseño de cualquier generador —de IA o determinista— cambiaría entero. Se midió con **tres implementaciones independientes** que no colaboraron entre sí y coincidieron, incluidos los 3 puentes exactos que produce el calendario (2026-06-22, 2026-12-07, 2027-04-09), que es donde más fácil era equivocarse. Las cifras de (b): en junio+julio+agosto hay **92 puestos de Pequeño** y solo 4 R2 para cubrirlos, porque INV-11 saca a los R1; el techo con nadie pasando de 6 son 4×6×3=**72**, así que faltan **20 días** y cada verano o alguien pasa de 6 (aviso INV-2) o hay días sin Pequeño (aviso INV-1 por infracobertura, V-12) — no hay tercera opción, y con 6 R2 sí cabría. Fuera del verano el problema se invierte: 273 puestos entre 8 Pequeños son 3,79/mes, por debajo del mínimo de 4. Esto es lo que convierte a V-14 en obligatorio y no en una preferencia: **el día que alguien suba INV-2 a `error`, el servicio se queda sin cuadrante de verano válido**, y por eso el test hermano lo fija con esa consecuencia escrita. El punto (c) sobre `total/f` salió de una discrepancia entre dos de las tres mediciones y es el hallazgo más caro de descubrir: un generador incremental que iguala totales **en crudo** falla el cierre anual en 0/6 intentos en cuanto hay una baja —un R3 que vuelve de tres meses con las mismas 51 guardias que sus compañeros lo lee el juez como 68,19 frente a 51—, mientras que el mismo generador dividiendo por disponibilidad sube a 4/6; igualar en crudo a quien vuelve de una baja es exactamente lo que INV-3 penaliza. Control negativo, que es lo que da sentido a todo lo anterior: un reparto **legal pero sin optimizar** produce del orden de 17 violaciones, así que la regla no es trivial de cumplir, es alcanzable si se persigue. Medido y NO cerrado *(el cierre **trimestral** se cerró luego en V-37)*: el cierre **trimestral** (V-13) no se juzgó ni una vez, ni se comprobó si el ±1 sobrevive al añadir INV-6/7/8/9/10, ni con residentes de la misma cohorte con ventanas distintas (nota [a]); y todo está medido sobre un año concreto, que tiene 3 puentes y 14 festivos |
| V-33 | **Cimientos previos a un generador determinista.** (a) `listPreferencias` (alcance EQUIPO, abierta a cualquier sesión como `listBloqueos`): la tabla `preferencias` era de **solo escritura** —`misPreferencias` filtra por `session.sub`— así que los residentes llevaban meses rellenando `fechasEvitar`/`maxGuardias`/`preferDobles`/`notas` y no las leía nadie, ni una pantalla ni un invariante. (b) El prompt del Generador gana tres secciones que faltaban: voluntarios del 3P (con su `desde`, que es lo que arranca el ciclo L-D de INV-8b, contrato C-4), eventos del servicio del curso, y las preferencias del equipo marcadas **explícitamente como BLANDAS**, para que el modelo no las confunda con bloqueos. (c) `comprobar()` pasa a asíncrono y comprueba lo mismo que `Calendar.jsx` (ver §7). Las preferencias no las lee ningún invariante y siguen sin leerlas: entran al prompt, no al validador — V-6/V-8 siguen intactas | Las tres son requisitos de cualquier generador, de IA o determinista, y ninguna dependía de decidir antes cómo será el generador. (a) es la más sangrante: pedirle a la gente que rellene un formulario que nadie lee es peor que no tenerlo, y el reparto no puede respetar una preferencia que el que reparte no ve. Se abre a cualquier sesión y no al permiso del ciclo (V-16) porque son datos BLANDOS —no bloquean nada, a diferencia de las ausencias, que ya son visibles para todos— y porque restringirla habría dejado al generador sin ellas justo cuando no hay Responsable, que es el caso que V-16 existe para desbloquear. (b) tapa dos huecos que se apagaban solos: la norma del 3P pedía «equidad entre voluntarios» sin decir quiénes eran —el modelo lo repartía entre cualquiera o no lo ponía— y la de eventos terminaba literalmente en «si no conoces esas fechas, ignora esta norma». Un fallo de red al cargar cualquiera de las tres **no tumba la pantalla**: cada sección dice que no hay datos, que es muy distinto de omitirla y dejar improvisar (mismo criterio que ya tenían los festivos, S-4) |
| V-34 | **Generador determinista propio (`v2/domain/schedule.js`), y nadie hace guardia dos días seguidos.** (a) `generateMonth(ctx, {semilla, pasos})` es puro, sin red y sin dependencias: recibe el MISMO `ctx` de `buildMonthContext` y devuelve una propuesta. Siembra greedy por `total/f` + búsqueda local con dos operadores, MOVER y intercambiar. (b) Escribe en el MISMO textarea que se pega a mano: no hay una segunda vía de escritura, así que el rechazo de fechas/ids y el reemplazo de mes de V-31 siguen aplicando igual. (c) El coste imita a `validateResidencyYearClose` —mismos seis ejes, misma normalización por `f`, misma agrupación por cohorte— pero el juez sigue siendo el validador; si divergen, manda el validador. `availabilityFraction` se **exporta** desde `equity.js` en vez de copiarse. (d) **Nadie coge guardia dos días seguidos**: restricción DURA del generador, por decisión del autor, que mira también el borde con el mes anterior. *(Superado por V-35: pasó a ser el invariante INV-15, `error`, así que ya no es solo del generador — el validador lo comprueba también en lo escrito a mano, y el generador dejó de ceder: el puesto se queda sin cubrir antes que proponer algo ilegal.)* (e) El prompt portátil se queda como escalón de degradación, no se retira | Las tres reglas de V-32 son el diseño entero, y cada una tenía un contraejemplo medido: perseguir el mes en vez del acumulado da 0/6 al cierre; igualar `total` en crudo da 0/6 en cuanto hay una baja; y un buscador de solo-intercambio no puede mover el eje `total` porque un intercambio conserva el número de guardias de cada uno. (b) es lo que evita el error clásico de añadir un camino nuevo que se salta los controles del viejo: el generador no aplica nada, solo rellena el cuadro. (d) sale de medir: sin la restricción salían ~9 pares de días seguidos al mes, legales pero inaceptables en la práctica; ceder antes que dejar un día vacío es V-14 otra vez —un día sin cubrir es `error` de INV-1 e impide validar el mes entero, dos guardias seguidas se corrigen a mano. Rendimiento: `bridgesBetween` dentro del bucle de coste era el 95% del tiempo (6,6 s → 0,2 s al izarlo fuera), y una parada por estancamiento evita gastar 20.000 pasos en el caso que no puede cuadrar (406 ms → 86 ms) sin perder ni un punto de calidad. Medido sobre 30 meses reales: los seis ejes dentro de ±1 en todos. 14 tests nuevos, juzgados SIEMPRE por el dominio real |
| V-35 | **INV-15 en vigor: nadie hace dos guardias en días consecutivos, y es `error`.** (a) Lo comprueba `validate.js` sobre el par de días, contando el **3P** (es «tercer puesto de guardia») y no las marcas V/R/B. (b) El par que cruza el borde del mes se juzga en el mes nuevo —el histórico ya llega por el contrato C-2—, pero un par ocurrido entero fuera del mes NO, o cada violación se duplicaría en todos los meses posteriores. (c) `schedule.js` deja de ceder: si nadie puede coger el día sin encadenar, el puesto se queda **sin cubrir** con `motivo: "descanso"` en vez de proponer la guardia consecutiva. (d) Sube de restricción interna del generador (V-30d) a invariante: ahora también se comprueba lo que se escribe a mano en la rejilla | Es `error` y **no es una excepción a V-14**: esa decisión bloquea «lo imposible y lo ilegal», y tras una guardia de 24 h el descanso del día siguiente es una obligación legal — mismo cajón que INV-5, no el de INV-2. Razón del autor, literal: «por ley tras una guardia de 24h no puedes hacer otra guardia seguida, necesitas hacer un descanso que es el día siguiente». La objeción que se le puso enfrente era de factibilidad, y la contestó con la plantilla real: 7 Mayores y 8 Pequeños este curso, 8 y 8 el siguiente, de sobra para cubrir el mes sin repetir aun contando bajas, rotaciones y vacaciones. Medido antes de preguntar: con la plantilla real (3 R4 / 4 R3 / 4 R2 / 4 R1) salen **0 días forzados** en los cinco meses probados, incluso con 2 de los 4 R2 de baja; solo aprieta de verdad cuando el pool elegible de un puesto baja a UNA persona (3 de 4 R2 de baja en verano, donde INV-11 saca además a los R1): esa persona solo puede hacer días alternos, así que la mitad de los días de ese puesto se quedan vacíos — 46 al año, medido. **Corrección del 2026-08-11:** la primera redacción decía que «ahí el mes deja de poder validarse», y es falso; medido con `validateMonth`, un día con el puesto de Pequeño vacío conserva al Mayor y sale como **aviso** de INV-1 («cubierta por 1 sola persona», V-12), no como error — 15 días vacíos producen 0 errores. Solo sería error un día con CERO personas, y eso exige que se vacíen los dos puestos a la vez, cosa que no ocurre porque el pool de Mayores no baja de 4 en ningún escenario probado. La consecuencia aceptada es real pero más benigna de lo que se escribió: el mes se puede validar, con avisos y la confirmación explícita de siempre. (c) es coherencia elemental: el generador no propone lo que el validador rechaza, igual que nunca propone sobre una baja ni un R1 en verano |
| V-36 | **La rejilla avisa del descanso de INV-15 en el momento de escribir la celda.** `client/lib/rest.js:chainedNeighbour` mira si la celda recién escrita encadena con el día anterior o el siguiente de ese mismo residente, y la pantalla lo dice con los dos días nombrados y un botón para deshacer. Cubre TODOS los códigos que ocupan puesto (G/GF/GP/3P), no solo el 3P: la regla es la misma para una G escrita a mano, y restringirla habría sido arbitrario. `v2/domain/validate.js` exporta `OCUPA_PUESTO` para que cliente y validador no tengan dos copias de qué códigos ocupan puesto — V/R/B no encadenan. NO bloquea (V-14): la celda se escribe igual. Arregla de paso que `validar()` solo cargaba histórico cuando había una rotación cercana, así que el par de INV-15 que cruza el borde del mes tampoco se veía **al validar**, pese a que el comentario del invariante da por hecho que el invocador aporta ese contexto: los dos días vecinos ya se cargan al abrir el mes y ahora entran en el ctx, deduplicados para no falsear el `propias` de INV-7 | El 3P lo pone SIEMPRE una persona a mano —`schedule.js` no lo reparte— encima de un mes que el generador ya dejó limpio, y el sitio libre es mucho menor de lo que parece. Medido sobre años generados: un residente con sus ~4,6 guardias del mes tiene el **42% del mes vetado** para un 3P (sus días más los dos vecinos de cada uno), y en el peor caso observado (un R2 con 8 guardias en agosto) le quedaban **6 días de 31**. Medido también el daño del flujo actual: colocando el 3P a ciegas, **uno de cada tres crea un error de INV-15**, y con 4 al mes el **74% de los meses** dejan de poder validarse. Como INV-15 es `error`, basta uno para bloquear la validación, y hasta ahora eso se descubría mucho después, al pulsar Validar, con un mensaje que habla de días consecutivos sin mencionar el 3P recién escrito — justo el dato que le falta a quien tiene que arreglarlo. Colocado BIEN (esquivando la adyacencia) el 3P no rompe nada: medido 6/6 en el ±1 con densidades de 1 a 30 al mes, porque `tally` lo excluye de los seis ejes (INV-4). **Verificado en vivo** contra `dev-server.mjs`: G el día 10 no avisa, 3P el 11 sí, deshacer lo limpia, V el 11 no avisa, y un 3P el 1 de agosto avisa del par con la G del 31 de julio — que es además el caso que Validar no veía |
| V-37 | **La tolerancia de INV-3 viaja con la normalización, y el cierre TRIMESTRAL entra por fin en el banco de equidad.** (a) Los dos cierres comparan ahora contra `1/min(f)` del par que se nombra, no contra un 1 fijo: el ±1 son UNA GUARDIA de verdad, y al dividir por la disponibilidad una guardia deja de valer 1 y pasa a valer `1/f`. La regla se escribe UNA vez (`equity.js:excedeElUno`) y la usan el cierre anual y el trimestral. (b) `test/bench/equidad-factible.mjs` juzga también con `validateQuarterClose`, que hasta ahora no había invocado ninguna medición —ni el banco ni los tests del generador—, y lo hace sobre los trimestres que la ventana contiene ENTEROS, diciendo cuál se queda fuera en vez de callarlo: T4 va de marzo al 31 de mayo y el año de residencia cierra el 26, así que **T4 cruza siempre el aniversario**. El banco se puede además importar sin disparar su informe, para poder sondearlo sin pagar sus minutos. (c) Lo medido: perseguir el año **no** trae el trimestre (el óptimo anual, 3/3 en el cierre anual, falla el trimestral en los seis escenarios y todas las semillas, 17-33 avisos), y `schedule.js` sobre 12 meses encadenados deja 3-4 desequilibrios trimestrales al año sin bajas y 4-6 con una — todo ello con CERO errores de `validateMonth` y cero puestos sin cubrir. Pero los dos cierres **sí son alcanzables a la vez**: pidiéndoselos al coste, salen los dos en los seis escenarios y sin errores duros, a cambio de entre 2,5× y 5× de tiempo de búsqueda. Al generador le falta objetivo, no espacio. (d) Se le da ese objetivo: `schedule.js` persigue además el trimestre EN CURSO hasta el fin del mes que genera (`trimesterWindow`, no un borde adivinado), con su propia `f` —la del trimestre, no la del año— y la tolerancia de (a). Medido sobre 12 meses encadenados: los avisos trimestrales pasan de 3-4 al año a **0** sin bajas y de 5-6 a **2** con una baja de 3 meses, **sin tocar el cierre anual** (0 violaciones en las seis pasadas) ni los errores de mes (0) ni la cobertura (0 puestos sin cubrir). Los 2 que quedan son la miopía ya conocida del incremental, no algo nuevo: a quien vuelve de la baja le falta en el trimestre anterior y le sobra en el siguiente, recuperando. Y no cuesta tiempo (~30 ms/mes, igual que antes) porque el término trimestral es O(1) y no añade un segundo `tally` — el 2,5×-5× que paga el banco es de buscar el año entero de golpe, no de esta cuenta | (a) Con el 1 fijo, una cohorte que comparte la misma `f` < 1 veía su ±1 **encogerse a `f`**, porque el cociente escala también la diferencia mientras la tolerancia se quedaba quieta — y entonces solo pasaba el empate exacto. No es un caso de laboratorio: la cohorte de R4 que termina la residencia el 26 de mayo tiene f = 87/92 en T4, de modo que **cada mayo** un reparto de 11 y 10 guardias (diferencia exactamente 1, o sea correcta) salía como `11,63 vs 10,57` y avisaba. Y como INV-3 está en `EQUITY_INVARIANTS`, obligaba al «Validar de todas formas» de V-14 **justo cuando el reparto era perfecto**: la app enseñaba a saltarse su propio aviso, que es lo último que le conviene a algo que tiene que sobrevivir sin administrador. El mismo agujero estaba en el cierre ANUAL (dos residentes con la misma baja de 3 meses, 20 y 21 guardias → `27,97 vs 26,64`, avisando en tres ejes). **La primera versión de esta decisión fue otra y estaba mal**, y queda escrito porque el error es instructivo: se probó a poner un suelo —«si la diferencia CRUDA no pasa de 1, no se avisa»—, que mata el artefacto igual de bien pero apaga la normalización donde hace falta, y lo delató el test que fija la regla 2 de V-32 (dar las MISMAS 30 guardias en crudo a quien estuvo tres meses de baja dejaba de ser violación, que es exactamente lo que INV-3 existe para penalizar). Escalar la tolerancia distingue los dos casos; el suelo no. (b) es la laguna que quedaba de V-13/P-8: el cierre trimestral llevaba desde entonces cableado en `marcarValidado` y en `closes.js` **sin que ninguna medición lo hubiera juzgado una sola vez**, así que nadie sabía si el generador lo cumplía. Medido ahora en (c): no lo cumple del todo, y el artefacto de (a) llevaba dentro todo ese tiempo sin que nadie pudiera verlo |
| V-38 | **El generador reparte también el 3.º puesto, y la cantidad la pone una persona.** (a) `generateMonth` acepta `tercerPuesto` (cuántos), `voluntarios3P` e `historial3P`, y coloca esos 3P sobre el mes ya resuelto. Van por OPCIONES y no por `ctx`: `ctx` es el que arma `buildMonthContext` para `validateMonth`, y a INV-8 no lo juzga `validateMonth` sino `validateThirdPost`. (b) El número **arranca en 0** y se pide en la pantalla, que solo lo ofrece si hay voluntarios apuntados. (c) Las cuatro reglas de INV-8 se respetan por construcción: 8a solo a quien constaba voluntario ese día; 8b con el ciclo L-D que juzga `thirdpost.js:thirdPostCycleRepeats`, **exportada** para que generador y validador no tengan dos versiones; 8c eligiendo entre candidatos al que menos 3P lleva en su año de residencia; y 8d colocando primero en días de mochila y **sin salir de ellos mientras quede alguno sin cubrir**, de modo que el aviso de 8d no puede llegar a existir. También INV-15, que es `error`: el 3P encadena igual que una guardia. (d) Si no caben todas, coloca las que caben y **dice por qué** (`diagnostico.tercerPuesto.motivo`), en vez de romper el ciclo. (e) `apply-month.js`: el 3P entra en los códigos borrables **solo si la propuesta trae 3P**. (f) Corrige que `schedule.js` filtraba el histórico con una lista propia de G/GF/GP en vez de `OCUPA_PUESTO`. (g) Corrige que `Generator.jsx` leía `diagnostico.adyacenciaForzada`, que V-35 había eliminado | (b) es lo único que la tabla no puede contestar: `voluntarios3P` dice **quién** se apunta, no **cuántos** quiere hacer, y la normativa insiste en «será siempre voluntario» — un generador que decidiera solo que alguien hace 16 turnos extra al mes estaría inventando un dato. Con 0 se comporta como antes y nada cambia sin que alguien lo pida. Medido para poder preguntarlo: ~15,7 días de mochila al mes fuera de verano y **0 en verano** (INV-11 saca a los R1), y el techo por voluntario lo pone el ciclo L-D, no la adyacencia — con un solo voluntario y la plantilla real caben 5 al mes de 30 pedidas. (c) el 3P se coloca DESPUÉS de las guardias y eso lo permite INV-4: `tally` lo excluye de los seis ejes, así que no puede estropear la equidad recién cuadrada. Al revés sí importaría — un 3P puesto antes bloquearía días por adyacencia y estrecharía el reparto de lo obligatorio por lo voluntario. (e) estaba fijo en la lista de borrables desde V-31, y como `schedule.js` no repartía 3P, **regenerar un mes se llevaba por delante los 3P puestos a mano** —los únicos que había— contándolos además como «guardias» en el aviso de borrado. Contradecía el comentario del propio fichero («los códigos que el generador propone y, por tanto, los únicos que le pertenecen»). (f) INV-15 cuenta el 3P desde V-35, así que un 3P del mes anterior veta el día 1: con la lista propia no lo vetaba y el generador **producía él mismo un `error` de INV-15**, medido, rompiendo su propia regla de no proponer nunca lo que el validador rechaza. Cuarta vez que una copia de un conjunto del dominio se desincroniza en silencio. (g) es peor de lo que parece y solo se veía en el navegador: `adyacenciaForzada` desapareció del diagnóstico en V-35 y la pantalla seguía leyendo su `.length`, así que **desde ese commit pulsar «Generar propuesta» dejaba la pantalla en blanco**. Ningún test lo habría cogido —los `.jsx` no son testeables en Node— y la rama llevaba así, con el PR abierto, desde entonces. El aviso que queda distingue ahora los dos motivos de `sinCubrir`, que V-35 separó justo para poder decirlo |
| V-42 | **Botón «↩️ Deshacer» general en `Calendar.jsx`, no solo para el aviso de descanso.** `aplica()` apila `{residenteId, fecha, codigoAnterior, origenAnterior}` antes de pisar cada celda (salvo cuando el propio Deshacer se reaplica a sí mismo); el botón hace `pop()` y reescribe el valor anterior con la misma `aplica()`, así que el reverso entra en `pendientes` igual que cualquier otro cambio y se confirma con el mismo Guardar de siempre — nada se revierte solo en el servidor. Funciona tanto sobre una celda recién tocada como sobre una ya guardada en una pulsación anterior de esta sesión (la pila no distingue guardado de pendiente). Se vacía al cambiar de mes, igual que `pendientes` | Pedido directo del autor (2026-08-18): «me añadí una guardia y me confundí» no tenía más salida que reconstruir la celda a mano ciclando de nuevo o borrarla y perder qué había antes. V-36 ya había resuelto el mismo problema para UN caso concreto (el aviso de descanso de INV-15) con un botón que borra esa celda; este generaliza el mecanismo a cualquier celda y restaura el valor anterior en vez de solo borrar, sin duplicar la pila de V-36 (los dos botones conviven: el de V-36 sigue siendo el atajo de "borra esta" desde el propio aviso) |
| V-41 | **El primer click sobre una celda vacía asigna G/GF/GP según el calendario de festivos, no siempre G.** `v2/domain/calendar.js:autoGuardCode(fecha, festivos)` — GF si `fecha` es festiva, GP si la SIGUIENTE lo es (víspera), G en cualquier otro caso. `Calendar.jsx` carga `festivos` del mes (con un día de margen para el 31 víspera de un festivo del mes siguiente) al abrir el mes, igual que ya hacía solo al validar, y lo usa cuando `cicla()` parte de una celda vacía. Ciclar SIGUE permitiendo forzar cualquier código a mano después (INV-12 nunca bloquea, V-14): esto solo cambia dónde aterriza el primer click, no quita control. Si `festivos` no se pudo cargar, un aviso lo dice y el primer click cae a G sin fingir que comprobó nada — igual que `autoGuardCode` sin lista (nunca inventa un festivo que no está cargado) | Pedido directo del autor (2026-08-18): ciclar a mano G→GF→GP para acertar el código de un día festivo o su víspera es el error más frecuente de la rejilla, y el dato para no tener que hacerlo ya vive en la tabla `festivos` que INV-12 usa para comprobarlo DESPUÉS — moverlo a ANTES no inventa nada nuevo, solo evita el ciclo manual en el caso común. Corrige a mano una GF/GP mal puesta sigue siendo tan fácil como añadir el festivo que faltaba en Datos del servicio (S-4: festivos son dato de entrada, se corrige ahí, no con un override paralelo en la rejilla) |
| V-40 | **INV-8a "¿era voluntario ESE día?" llega también a `Calendar.jsx`.** `estadoVoluntariado3P` devuelve ahora `periodos` (activos e históricos, vía `allThirdPostPeriods`) además de `voluntarios` (los activos de HOY); `client/lib/thirdpost.js` los pasa como `periodosVoluntario3P` a `validateThirdPost`, igual que `buildThirdPostCtx` hace en el servidor desde V-28. `voluntarios3P`/`historial3P` (8b/8c, sobre el compromiso vigente) no cambian | V-28 arregló el criterio en `validateThirdPost` y lo cableó en `marcarValidado`, pero `Calendar.jsx` seguía pidiendo solo `voluntarios` (la lista de HOY): la pantalla podía avisar en falso por un 3P de alguien ya retirado, o callar sobre uno de antes de apuntarse — el mismo bug que V-28 cerró, sin cerrar en el sitio donde el residente lo ve antes de pulsar Validar. Exactamente el riesgo de "dos veredictos distintos sobre el mismo mes" que el propio comentario de `thirdpost.js` (cliente) ya advertía |
| V-39 | **La miopía del incremental con bajas largas: medida de nuevo, un trozo arreglado y el resto localizado.** (a) `schedule.js` calculaba los puentes libres con un filtro propio («no lo trabajó») en vez de `equity.js:residentIsFreeOnBridge`, que desde V-27 **no cuenta como libre el puente caído dentro de una ausencia**. Corregido usando la del dominio. (b) **Previsión sobre el eje escaso**: el eje `puentesLibres` se mide ahora sumando también los puentes que a cada residente le QUEDAN por delante y podrían ser suyos. Solo se proyecta este eje. (c) Lo que queda: con una baja de 6 meses, el eje `total` se queda a UNA guardia del ±1. **No es límite del invariante ni del enfoque incremental**: con la parada por estancamiento quitada y 200.000 pasos sale a 0 violaciones — pero tarda 169 s frente a 3,6, o sea 47×, y el generador corre en el navegador. Queda como pregunta abierta, medida. (d) Se probó y se **descartó** una búsqueda dirigida (la mitad de los pasos moviendo del que más lleva en el eje peor al que menos): empeoró el conjunto —«dos bajas simultáneas» pasaba de 6/6 a 1/3— y no se queda | El diagnóstico que traía este punto —«cada guardia de alguien con f=0,75 vale 1,34 normalizada contra una tolerancia de 1,00»— **era el artefacto que arregló V-37**, así que lo primero fue volver a medir en vez de diseñar sobre él: la baja de 3 meses pasó de fallar siempre a salir limpia sin tocar nada. Medido sobre 12 meses encadenados con la plantilla real, cinco escenarios × 6 semillas, cierres ANUALES limpios: **20/30 antes, 20/30 con solo (a), 23/30 con (a)+(b)**. El desglose importa más que el total y se deja escrito porque hay regresiones reales: (a) por sí solo arregla a medias «dos bajas simultáneas» (0/6 → 3/6) y empeora «baja de 9 meses» (6/6 → 4/6); (b) encima remata «dos bajas» (3/6 → **6/6**), recupera «baja de 6 meses» (1/6 → 2/6) y cuesta una semilla en «baja de 3 meses» (6/6 → 5/6). (a) no era opcional aunque su saldo aislado sea neutro: es corrección, no heurística — el generador estaba acreditando puentes que el juez no le cuenta, o sea persiguiendo otro objetivo, que es la QUINTA copia de una regla del dominio que se desincroniza en silencio en dos días. Y lo que empeora no es que introduzca desequilibrios: es que **deja de esconderlos**. (b) se proyecta SOLO `puentesLibres` y no por comodidad: los otros cinco ejes cuentan lo que sí ocurrió y sus oportunidades futuras son muchas y compartidas, así que suponer que alguien se las llevará todas no informa de nada; este cuenta lo que NO ocurrió sobre un calendario cerrado y conocido de antemano —3 puentes en todo el año—, y ahí una sola decisión mal puesta en junio ya no se deshace en diciembre. (c) el ×10 de presupuesto no cambiaba ni el resultado ni el TIEMPO, que es la firma de una parada temprana y no la de un óptimo difícil: la parada por estancamiento (2.000 pasos sin mejora estricta) corta antes. Quitarla resuelve pero multiplica el tiempo por 47, y el arreglo bueno no es buscar más sino buscar mejor. (d) se deja escrito el intento fallido para que no se repita: dirigir la mitad de los pasos al par (máximo, mínimo) del peor eje suena razonable y mide peor, porque gasta en un movimiento estrecho y a menudo ilegal la exploración que sacaba a la búsqueda de los óptimos locales |
| V-45 | **La generación del cuadrante se hace en el SERVIDOR con un modelo Gemma (Gemini API), y el validador sigue mandando.** (a) Acción nueva `generarCuadranteIA` en `router.js`: comprueba permiso, arma el contexto del mes con el MISMO `buildCuadranteCtx` que ya usa `marcarValidado`, pide propuesta al modelo, la valida y solo entonces escribe. (b) El puerto es `deps.llm`; el adaptador impuro vive donde viven todos, en `Code.gs` (`UrlFetchApp` contra `generativelanguage.googleapis.com`). La clave sale de `PropertiesService.getScriptProperties()` (`GEMINI_API_KEY`) y viaja en la cabecera `x-goog-api-key`, nunca en la URL ni en el navegador; el modelo es configurable (`GEMINI_MODEL`, defecto `gemma-4-31b-it`). (c) El núcleo es PURO y testable: `server/src/ai-prompt.js` (arma el prompt y PARSEA la respuesta) y `server/src/ai-generator.js` (el ciclo de reintentos, con `llm` y `validar` inyectados). Ninguno de los dos toca la red. (d) **El juez sigue siendo el validador** («la IA propone, el validador dispone»): `validateMonth` + `validateThirdPost` sobre la propuesta. Solo `severidad: error` impide guardar (`canValidate`, V-14); los avisos se le devuelven igualmente al modelo para que mejore, pero no bloquean, exactamente como no bloquean a una persona. (e) **Máximo 3 intentos.** Cada reintento le devuelve al modelo su propia propuesta anterior y la lista CONCRETA de violaciones del validador, no un «vuelve a intentarlo». Si el tercero sigue con errores no se escribe **ni una fila**: se registra `REVISION_MANUAL` en la tabla nueva `generaciones` y se dice en pantalla. (f) Escribe por el MISMO camino que ya existía: `monthReplacementPlan` —que sube de `client/lib/apply-month.js` a `v2/domain/apply.js` para que el servidor pueda usarlo— más `appendRecords("asignaciones")` y `stateAfterEdit`. No hay una segunda vía de escritura, así que el guardarraíl de V-31 (reemplazo del mes, rechazo de fechas ajenas e ids inventados) se aplica ahora también a la IA. (g) El permiso es `requireCicloPermiso` (V-16), no un `rol === "responsable"` nuevo; en el cliente lo decide `puedeMoverCiclo`. (h) `client/screens/Generator.jsx` y su pestaña del BottomNav desaparecen: la generación vive solo en el botón de Inicio | Encargo directo del autor (2026-08-30). La premisa del ADR-001 («sin API de terceros, generador *prompt portátil*») era que no había dónde guardar un secreto: el cliente es estático y público. Con el backend de la Fase 2 sí lo hay, y la clave nunca sale de las Script Properties — el requisito rector que se defendía (cero secretos en el navegador) se sigue cumpliendo literalmente, así que lo que cambia es el medio, no el principio. (d) y (f) son lo que evita el error clásico de estrenar un camino nuevo que se salta los controles del viejo: el modelo no valida, no escribe y no decide nada — propone. (e) sale del encargo y es también V-14 otra vez: antes que guardar un cuadrante ilegal, ninguno. **Contrapartida registrada, no olvidada:** se pierde el escalón de degradación que V-34(e) dejaba explícito. `v2/domain/schedule.js:generateMonth` sigue en el dominio, en el bundle y probado, pero deja de ser alcanzable desde la interfaz: si mañana caduca la clave o Google retira el modelo, el servicio cae a la rejilla manual de `Calendar.jsx`, no a un generador. Es un riesgo aceptado del encargo (la app sigue funcionando entera sin IA), no un descuido — y devolver el escalón es media hora de trabajo el día que se decida |
| V-46 | **El generador con IA (V-45) deja de ofrecerse sobre un mes VALIDADO, y el autor de la app tiene acceso de desarrollador SOLO para esa acción.** (a) `puedeGenerarCuadrante` (cliente) y `handleGenerarIA` (servidor) restringen el botón/la acción a `estado === "BORRADOR"`; sobre VALIDADO se rechaza con un mensaje explícito, igual que ya pasaba con PUBLICADO. (b) `esAccesoDesarrolladorIA` (mismo nombre y criterio en cliente y servidor: email exacto) destraba el PERMISO del ciclo únicamente para `generarCuadranteIA` — no toca `requireCicloPermiso` ni `puedeMoverCiclo`, así que validar/publicar/despublicar/excepciones/sorteo/imaginaria siguen exigiendo Responsable o Mayor tal cual. Identificado por email, no por rol ni nivel, para no depender de nada que la app derive de fechas | Pedido directo del autor (2026-09-02). (a): una vez que el equipo se reúne y valida el mes entre todos, regenerarlo por encima lo descartaría en silencio — el mismo criterio que ya impide regenerar un PUBLICADO, extendido un estado antes. (b): el autor es R1/R2 (Pequeño) hoy y quiere poder probar el generador sin depender de tener el mandato de Responsable ni ser Mayor — pero forzarle el nivel o el grupo para conseguirlo falsearía un dato que se deriva de fechas reales y alimenta INV-11 y otros invariantes, así que el acceso se resuelve aparte, por identidad, y acotado a una sola acción en vez de ampliar el permiso del ciclo en general |
| V-47 | **El generador con IA (V-45) COMPLETA el mes por defecto en vez de reemplazarlo: las guardias que ya están en la rejilla son inamovibles.** (a) Cada residente apunta en su propia fila del cuadrante, antes de que se genere el mes, las guardias que ya tiene comprometidas (cambios acordados, días fijados con el servicio); la rejilla marca la fila propia (★) y Preferencias señala el camino. No hay tabla ni campo nuevo: la guardia fijada ES una fila de `asignaciones` como cualquier otra, y así la ven también el validador y la pestaña publicada. (b) `generarCuadranteIA` gana `modo`: `completar` (defecto) y `reemplazar` (lo de V-45). En `completar`, `v2/domain/apply.js:monthCompletionPlan` —hermano de `monthReplacementPlan` con la regla contraria— separa lo existente en *fijadas* (G/GF/GP/3P) y *marcadores* (V/R/B), y NUNCA emite una fila de borrado; las fijadas van al prompt en una sección propia («GUARDIAS YA FIJADAS», norma 14) pidiendo al modelo que las repita tal cual, y al escribir se descartan las repetidas (misma clave y código), con lo que conservan su `origen`. (c) **El validador juzga el mes RESULTANTE** (fijadas + lo que la propuesta añade), no la propuesta sola: si el modelo ignora una fijada y pone a otro Mayor ese día, INV-1 lo ve. Pisar una fijada con otro código es un `FORMATO` (error) que vuelve al modelo en el reintento, como las fechas de otro mes y los ids inventados (V-31). Un mes ya completo devuelto tal cual no escribe nada ni cambia de estado. (d) La bitácora `generaciones` gana la columna `modo` (al final, para no descolocar las hojas ya creadas) | Pedido del autor (2026-09-04): «que los propios residentes pongan sus guardias, que ya han puesto de antemano, en el calendario, cosa a tener en cuenta al generar con IA». Hasta aquí la app permitía ponerlas —la rejilla es editable por cualquiera— pero el generador las borraba (`monthReplacementPlan` existe para que el mes quede *exactamente* como la propuesta) y el prompt ni las mencionaba: la información que había que respetar era justo la que se perdía. Se conserva `reemplazar` porque regenerar un mes montado entero por la IA con «completar» no haría nada (todo sería fijado) y no hay otro gesto para vaciar un mes; se exige `modo` de una lista blanca porque un modo mal escrito no puede degradar en silencio al que borra. Se descartó una tabla/campo «fijada» aparte: dos fuentes de verdad para la misma guardia es la clase de duplicación que S-1/§1 («derivar, no almacenar») prohíbe, y lo que el validador lee es `asignaciones` |
| S-1 | Fechas ISO string + UTC, meses 1–12 | Mata la clase de bug del v1 (desfase +1 mes, `navMes` incoherente) |
| S-2 | Nivel por aniversario personal, no por año académico | Normativa mide equidad por año de residencia individual; el Excel (año-entero) no puede expresar la nota [a] |
| S-3 | Hueco entre periodos = nivel anterior; solape = inválido | Baja retrasa promoción; no existe des-promoción |
| S-4 | Festivos como datos, jamás derivados por IA | El v1 delegaba festivos al modelo: alucinables |
| S-5 | Doblete de borde de mes contado por fechas reales, atribuido al mes del viernes | El Excel los pierde; la equidad anual es la que manda |
| S-6 | Cero dependencias en el dominio (`node:test`, ES modules) | Durabilidad 10 años: sin toolchain que se pudra |
| S-7 | Código en inglés, dominio documentado en español (JSDoc), códigos G/GF/GP/3P/V/R/B literales | Convención del proyecto |

## 7. Estado de implementación

- [x] `v2/domain/calendar.js` — calendario puro (S-1)
- [x] `v2/domain/residents.js` — periodos, nivel, grupo, activo (S-2, S-3)
- [x] `v2/domain/tally.js` — contaje (§4), 19 tests
- [x] `v2/domain/validate.js` — `validateMonth`: INV-1,2,5,6,7,9,10,11 (§5), 63 tests
- [x] `v2/domain/thirdpost.js` — **INV-8 en vigor** (decisión V-18): deja de ser dominio que no invoca nadie. Estaba implementado y probado desde la Fase 3, pero ni `marcarValidado` ni ninguna pantalla lo llamaban, y no se podía cablear porque sin tabla de voluntarios marcaba TODO 3P como no-voluntario. Ahora: (1) las cuatro reglas son `aviso` —8a y 8b bajan de `error`—, así que ninguna bloquea, pero INV-8 sigue en `EQUITY_INVARIANTS` y por tanto exige la confirmación explícita de V-14; (2) tabla `voluntarios3P` (append-only, `desde` de tipo `"date"`, `compromisoAceptado`, `activo` para retirarse por reinserción) — **sin `periodoInicio`**, a diferencia de `voluntariosResponsable`, porque el 3P no se elige por periodos comunes: cada uno se apunta el día que quiere y su ciclo L-D arranca ahí; (3) `thirdPostHistoryStart` decide el histórico a leer (el `desde` de cada voluntario para 8b, la ventana del año de residencia para 8c, la más antigua de las dos), simétrico de `rotationHistoryStart`/C-2 y `yearCloseHistoryStart`; (4) `calendar.js` gana `addMonths`, y `thirdpost.js` `thirdPostCommitmentEnd`/`canWithdrawThirdPost`/`THIRD_POST_PERMANENCIA_MESES` para el compromiso de permanencia. Backend: acciones `estadoVoluntariado3P`/`ofrecerse3P`/`retirarVoluntariado3P` (autoservicio puro: la acción ignora cualquier `residenteId` del cliente y escribe siempre `session.sub`) y `buildThirdPostCtx` en `marcarValidado`. Cliente: `client/lib/thirdpost.js` (módulo `.js` real y testeable, al estilo de `closes.js`, que nunca da por comprobado lo que falló al cargar), usado por `Calendar.jsx` en paralelo con los cierres, y una tarjeta de autoservicio en `Prefs.jsx` con el disclaimer de permanencia que hay que aceptar para que se habilite el botón. 36 tests nuevos (15 dominio + 9 `client/lib` + 12 router), más 6 tests existentes reencuadrados al dejar INV-8 de tener severidad `error` y la paridad del bundle ampliada a las cuatro funciones nuevas. **Verificado en vivo** contra `dev-server.mjs`: alta con disclaimer, retirada bloqueada por permanencia («Apuntado desde el 2 ago, tu compromiso llega hasta el 1 dic»), y un 3P de quien no consta voluntario apareciendo bajo «Avisos — informativos», no bajo «Errores — bloquean la publicación»
- [x] `v2/domain/equity.js` — `validateResidencyYearClose`: INV-3 + INV-4 (cierre de año), 15 tests
- [x] **Festivos y INV-12** (decisión V-17) — cierra S-4. Tabla `festivos` (append-only, `fecha` de tipo `"date"`, `ambito` informativo, `activo` para anular por reinserción; `anio` NO se almacena, se deriva). `v2/domain/calendar.js`: `isHoliday` (consulta, nunca deriva) y `bridgesOfMonth` (§3.4 literal: laborable L-V no festivo entre dos no laborables, con los vecinos del día 1 y del último día tomados FUERA del mes). `v2/domain/validate.js`: rama INV-12, siempre aviso, con `festivos` entrando por `buildMonthContext`. Backend: `listFestivosRango` (abierta a cualquier sesión: la necesitan validador, puentes y prompt), `crearFestivos` (lote, un solo lock) y `anularFestivo`, las dos últimas con el permiso de V-16. Cliente: `Calendar.jsx` los carga al validar y `Generator.jsx` los mete en el prompt junto a los puentes derivados — antes le pedía al modelo distinguir GF/GP sin darle una sola fecha festiva, que es exactamente lo que S-4 prohíbe. 15 tests nuevos
- [x] **Eje `puentesLibres` de INV-3** (fase 3 de V-17) — cierra la laguna que V-13(e) dejó anotada: `buildYearCloseContext` lo fijaba a 0 y no pasaba ningún puente, así que ese eje del cierre anual comparaba ceros y salía «cuadrado» sin haber mirado nada. Ahora los puentes se DERIVAN de la tabla `festivos` (V-17b) sobre la ventana entera del año de residencia, no sobre el mes: `v2/domain/calendar.js` gana `bridgesBetween(desde, hasta, festivos)` (itera `bridgesOfMonth` y recorta por los extremos) y `v2/domain/equity.js` gana `yearCloseFestivosRange` — el rango de festivos que hay que leer, con un día de margen a cada lado, simétrico de `yearCloseHistoryStart` (contrato C-3). La ventana **cruza dos años naturales** porque el aniversario cae en mayo: el puente de diciembre pertenece al cierre del mayo siguiente. El reparto acumulado/mes se mantiene: los puentes anteriores al mes validado salen de `historicas` (el store) y los del mes de `asignacionesDelMes` (lo que hay en pantalla, ediciones sin guardar incluidas). Un 3P en un puente NO se lo quita a quien lo hace (INV-4: es voluntario); una cedida/comprada sí, porque quien tiene la fila trabaja ese día. **Si falta el calendario, se avisa de que el eje no se ha podido comprobar** en lugar de callar, y la comprobación es **por año natural**, no «¿hay algún festivo en la ventana?»: como la ventana cruza siempre dos años y `crearFestivos` carga un año de golpe (V-17a), «2026 cargado y 2027 no» es el estado intermedio normal del sistema, y la pregunta laxa daba por comprobados los puentes de 2027 con solo tener un festivo de 2026 — el eje habría vuelto a comparar ceros en silencio, que es justo lo que esta fase venía a cerrar (lo encontró la revisión adversarial de 50 agentes, cuatro lentes por separado). Por eso `yearCloseFestivosRange` devuelve los **años naturales completos** que toca la ventana (±1 día), no la ventana recortada: sin ver el año entero no se puede distinguir «ese año no está cargado» de «ese tramo del año no tiene festivos». Aquí no aplica el matiz de V-17(d) —no gritar cada febrero— porque la unidad ya no es el mes: un año natural español entero sin ningún festivo no existe. El aviso solo se emite si además hay alguna cohorte con ≥2 miembros, es decir si el eje se iba a comparar con alguien. Backend: `closeViolations` filtra `snap.festivos` por ese rango. Cliente: `closes.js` pide `listFestivosRango` solo cuando el mes cierra año (el trimestral mide únicamente `total`, V-13a) y un fallo de red NO se degrada a «no hay puentes». 20 tests nuevos (14 dominio + 5 `closes.js` + 1 router), verificados por mutación (poner el eje a 0 pone 7 en rojo) y con la paridad del bundle ampliada a las dos funciones nuevas
- [x] `v2/domain/absences.js` — **lector único de ausencias** (decisión V-19). Sustituye cinco filtros en línea con criterios distintos (INV-5, INV-2, INV-6, INV-7, descuento de INV-3) y el rango del router por un `absences(bloqueos, criterios)` con los conjuntos de motivos nombrados por el invariante que los usa, más `isNearbyRotation` (la lista de provincias vivía duplicada entre `rotationHistoryStart` e INV-7). Descarta `activo=false` en el dominio, que antes dependía de que cada invocador se acordara. Backend: `crearBloqueo` acepta `residenteId` ajeno y `cancelarBloqueo` una fila ajena, las dos con el permiso del ciclo (V-16). Cliente: `client/lib/permisos.js` (la regla de V-16 en un solo sitio, compartida por `Calendar.jsx` y `Prefs.jsx`) y, en Preferencias, el selector «¿de quién es esta ausencia?» con la lista de las ajenas del mes y su botón de cancelar. 19 tests nuevos (10 dominio + 9 router). **Verificado en vivo**: Carlos (R3, sin Responsable designado) registra una baja de Iván, se le asigna una G dentro de ella y el validador la rechaza con «[INV-5] Iván Cortés — Asignación G el 2026-08-01 sobre bloqueo BAJA» como error bloqueante. De paso, la verificación en navegador destapó un fallo que ningún test cogía: `server/dev-server.mjs` enumera los módulos del dominio A MANO y le faltaba `absences` (en producción no pasa: `Code.gs` pasa el `Domain` entero del bundle)
- [x] **INV-8a juzga "¿era voluntario ESE día?", no "¿lo es ahora?"** (decisión V-28) — `validateThirdPost` gana el parámetro opcional `periodosVoluntario3P` (`{residenteId,desde,hasta?}[]`, todos los periodos, activos e históricos). Sin él (los invocadores/tests viejos) se conserva el criterio anterior a propósito. Backend: `allThirdPostPeriods` lee `voluntarios3P` con `readRecords` (crudo, sin colapsar — `readLatest` se queda con una sola fila por residente y por eso perdía el periodo de en medio de quien se apuntó, se retiró y volvió a apuntarse) y funde cada alta con su baja por `residenteId+desde` (la baja siempre reinserta el mismo `desde` que su alta). `buildThirdPostCtx` lo cablea hasta `marcarValidado`; 8b/8c siguen usando la lista de HOY sin cambios, a propósito (el ciclo y el cierre de equidad sí son sobre el compromiso vigente). 7 tests nuevos (4 dominio + 3 router de punta a punta) y una fixture preexistente corregida (`router-cuadrante.test.js`: un 3P fechado 2 días ANTES del alta del voluntario pasaba por casualidad con el criterio viejo). Sigue siendo `aviso`, nunca bloquea (V-18)
- [x] Duplicado de la laguna que V-33 ya cerró (ver más abajo, "`Generator.jsx` ya comprueba lo mismo que `Calendar.jsx`") — este punto se quedó sin tachar cuando esa decisión entró. `comprobar()` (`client/screens/Generator.jsx`) ya es asíncrono y llama en paralelo a `closeViolations`/`thirdPostViolations` antes de fijar el veredicto
- [x] **`puentesLibres` deja de sesgarse por ausencias** (decisión V-27) — dos correcciones, aplicadas juntas (decisión explícita del autor: hacer solo una de las dos las hace tirar en sentidos opuestos, ver la propia decisión). (1) `residentIsFreeOnBridge` gana `bloqueos` y excluye del recuento cualquier puente cubierto por BAJA, VACACIONES o ROTACION (`absences.js:AUSENTE_EN_PUENTE`, los tres motivos — no solo BAJA): antes, cualquier ausencia larga hacía «libres» todos los puentes de ese tramo sin límite. (2) `puentesLibres` entra en `PROPORCIONAL` y se divide por la misma `f` (BAJA únicamente, nota [a], sin cambios en V-8) que ya usan `total`/`findes`/`festivos`/`prefestivos`/`dobletes`. 4 tests nuevos (2 de la exclusión vía `buildYearCloseContext`, 1 de la normalización con un ejemplo donde el diff crudo violaría pero el normalizado no, y la suite existente de 44 tests de equidad sigue en verde sin tocar nada). Sigue siendo aviso, nunca bloquea (V-14)
- [x] **INV-8a ya juzga «¿era voluntario ESE día?» también en `Calendar.jsx`** (decisión V-40) — el servidor (`marcarValidado`) lo hacía desde V-28, pero la pantalla pedía solo la lista de voluntarios de HOY (`estadoVoluntariado3P` → `voluntarios`) y podía dar un veredicto distinto al del servidor para un mes pasado. `estadoVoluntariado3P` devuelve ahora también `periodos` (activos e históricos), que `client/lib/thirdpost.js` pasa como `periodosVoluntario3P`
- [x] **El primer click de una celda vacía ya asigna G/GF/GP según festivos, no siempre G** (decisión V-41) — `v2/domain/calendar.js:autoGuardCode`, cableado en `Calendar.jsx:cicla()`. Cierra el ciclo manual G→GF→GP que hasta ahora exigía acertar a mano el código de un festivo o su víspera
- [x] **Botón «↩️ Deshacer» general en `Calendar.jsx`** (decisión V-42) — generaliza el botón de V-36 (que solo borraba la celda del aviso de descanso) a cualquier celda tocada en la sesión, guardada o no, restaurando el valor anterior en vez de solo borrar
- [x] **`Generator.jsx` ya comprueba lo mismo que `Calendar.jsx`** (decisión V-33) — cierra esta laguna, abierta desde P-8. `comprobar()` es asíncrono y llama a `client/lib/closes.js` y `client/lib/thirdpost.js` además de `validateMonth`, y pasa por fin `eventos` a `buildMonthContext` (INV-10 corría siempre con la lista vacía en esta pantalla, así que no podía emitir un solo aviso por mucho que la tabla estuviera rellena). Lo que la red no consiga comprobar se **nombra y se pregunta** («Aplicar de todas formas»), nunca se da por bueno ni bloquea en seco (V-14). La red obliga además a un guardarraíl de vigencia que la versión síncrona no necesitaba: `runRef` numera cada pasada y `respuestaVigenteRef` (escrita **en render**, no en un efecto, que corre demasiado tarde) guarda el texto real del textarea; si al volver no coinciden, el veredicto se tira. **Verificado en vivo** contra `dev-server.mjs` con un retardo artificial de 2,5 s: un 3P dado a Carlos, que no es voluntario, produce «[INV-8] 3P asignado a Carlos Ruiz, que no consta en la lista de voluntarios» —imposible antes de este cambio—; con la sesión rota, la pantalla dice «No se ha podido comprobar la equidad de cierre (INV-3) ni el tercer puesto (INV-8)», retira el INV-8 que ya no puede afirmar y exige el segundo clic (que no escribe nada hasta darlo); y reescribir la respuesta con la comprobación en vuelo descarta el veredicto viejo sin dejar el botón colgado en «Comprobando…»
- [x] **INV-15 en vigor** (decisión V-35) — `validate.js` comprueba el descanso tras guardia sobre el par de días (3P incluido, marcas V/R/B no), con el borde del mes dentro y el par de fuera del mes fuera. `schedule.js` deja de ceder. Sube de restricción del generador a invariante: la rejilla hecha a mano también se comprueba. 5 tests nuevos de validador + los del generador reescritos. **Consecuencia aceptada y medida**: si el pool elegible de un puesto baja a una persona (3 de 4 R2 de baja en verano), el mes deja de poder validarse — con la plantilla real (7 Mayores, 8 Pequeños) no ocurre en ninguno de los meses probados
- [x] **Generador determinista** (decisión V-34) — `v2/domain/schedule.js:generateMonth`. Puro, sin red, sin dependencias y determinista (PRNG con semilla; `Math.random` no entra aquí, o dos ejecuciones del mismo mes no serían comparables). Aplica las tres reglas de V-32: persigue el acumulado del año de residencia, compara `total/f` y reasigna además de intercambiar. Respeta por construcción lo que produce `error` —INV-1 (1 Mayor + 1 Pequeño), INV-5 (baja) e INV-11 (R1 en verano)— y los códigos que espera INV-12. Respeta también INV-15 (nadie dos días seguidos, borde con el mes anterior incluido), que desde V-35 es invariante y `error`: si nadie puede coger el día sin encadenar, deja el puesto sin cubrir antes que proponer algo ilegal. Entrega en el mismo textarea que se pega a mano, así que `comprobar()`/`aplicar()` y el guardarraíl de V-31 no cambian. **Verificado en vivo** contra `dev-server.mjs`: genera agosto-2026, `comprobar()` no saca un solo error (solo el INV-2 esperado), se aplica y quedan 62 guardias con **cero** pares de días seguidos entre los tres Mayores — los 30 de Elena son los inevitables (única R2 elegible en verano) y la pantalla los anuncia antes de aplicar
- [x] **`schedule.js` ya propone 3P** (decisión V-38) — cierra la laguna anotada aquí: el generador acepta `tercerPuesto`/`voluntarios3P`/`historial3P` y coloca los 3P DESPUÉS de resolver el mes obligatorio, respetando por construcción las cuatro reglas de INV-8 (8a voluntarios, 8b ciclo L-D vía `thirdPostCycleRepeats` exportada, 8c menor acumulado, 8d prioridad mochila sin salir de ella) e INV-15 (descanso, `error`). La cantidad la decide una persona en pantalla, nunca el generador solo — la tabla dice QUIÉN se apunta, no CUÁNTOS quiere hacer
- [x] **Ausencias y `puentesLibres` ya no sesgan el eje** (decisión V-27, reforzada en V-39) — `residentIsFreeOnBridge` (`equity.js`) descarta como libre cualquier puente dentro de BAJA/VACACIONES/ROTACION (`AUSENTE_EN_PUENTE`), y `puentesLibres` está en `PROPORCIONAL` (se normaliza por disponibilidad igual que los demás ejes). `schedule.js` usaba un filtro propio en vez de esta función; V-39(a) lo corrigió para que el generador persiga el mismo objetivo que el juez. Ver CLAUDE.md C-3 y los tests de `equity.test.js` ("Sesgo de las ausencias en `puentesLibres`")
- [x] **INV-10 en vigor** (decisión V-20) — deja de estar MUDO. Tabla `eventos` (append-only, `activo`, calcada de `festivos`), acciones `listEventos`/`crearEvento`/`anularEvento`/`sortearEvento` (las tres últimas con el permiso del ciclo, V-16), y las filas llegando al validador por `buildMonthContext`, que las moldea y **empareja Navidad y despedida por año académico** — sin eso, validar mayo no encontraría la Navidad de diciembre con la que comparar. `sortearEvento` hace el sorteo de verdad reutilizando `drawResponsible` y dejando fila en `sorteos` con semilla y candidatos: recomputable, como el del Responsable. `designadosNavidad` deja de ser un parámetro suelto de `validateMonth` (nadie se lo pasaba: medio motivo de que INV-10 llevara mudo desde la Fase 3) y sale de la fila del evento
- [x] **INV-13 como herramienta** (decisión V-20) — `v2/domain/imaginaria.js`: `imaginariaQueue` (cola derivada del historial, con el motivo de quien queda apartado para que la pantalla diga también a quién NO llamar) y `nextForImaginaria`. Tabla `imaginaria` (append-only, `activo`). Acciones `colaImaginaria` (abierta: cualquiera puede necesitar saber a quién llamar), `registrarImaginaria` y `anularImaginaria` (permiso del ciclo). Cliente: tarjeta en Inicio con grupo, fecha y la cola numerada. **No hay validación a posteriori** y `validateMonth` no lo llama: es deliberado, ver V-20(b)
- [x] **Pantalla de carga de festivos y eventos** (`client/screens/DatosServicio.jsx`, enlazada desde Ajustes) — cierra la laguna que dejaban V-17 y V-20: las dos tablas estaban en vigor y sin forma de rellenarse desde la app, así que INV-10 e INV-12 se comprobaban contra tablas vacías. Festivos: pegado **en lote** por año (V-17a, «se pega de golpe»), con vista previa que separa lo que se va a escribir de lo que no y por qué, con el número de línea real; el parseo vive en `client/lib/festivos-parse.js` (módulo `.js` testeable, 10 tests) porque una línea mal leída no da error visible, mete una fecha equivocada en la tabla que INV-12 usa. Se enseñan además los **puentes derivados** del año: un año sin ningún puente suele ser un año a medias, y es la única señal de que la carga está bien. Eventos: alta, sorteo reproducible de los 2 R2 (`sortearEvento`) y anulación, con los voluntarios elegibles limitados a los R2 de esa fecha. Escribir exige el permiso del ciclo (V-16) y a quien no lo tiene se le dice, en vez de esconderle la pantalla. **Verificado en vivo**: pegado de 6 líneas con 3 válidas y 3 rechazadas (fecha laxa, repetida, ámbito inventado) con su número de línea; los 3 festivos cargados derivan el puente del 7 de diciembre; el sorteo se niega con «hacen falta al menos 2 R2 (hay 1)»; y un R2 ve los datos sin un solo control de escritura
- [x] **La rejilla del cuadrante deja de agruparse por el nivel de hoy** (decisión V-23) — `client/screens/Calendar.jsx` derivaba el nivel con `todayISO()` y `porNivel` solo admite R1..R4, así que la pantalla y `validateMonth` opinaban distinto sobre el mismo residente: dibujando junio-2027, dos R4 que terminaron el 2027-05-21 salían etiquetados «R4» y asignables, mientras el validador los juzgaba FINALIZADO. Y hacia atrás era peor: un FINALIZADO hoy desaparecía de **todos** los meses, incluidos los pasados en los que sí hizo guardias — filas que no se podían ni ver ni corregir. Ahora se usa el ancla de `projection.js` (activo algún día del mes, etiquetado por su primer día activo), que además alinea la rejilla con la pestaña publicada. Los no asignables con algo escrito salen en una fila naranja al final cuyas celdas solo borran, con la causa y su fecha frontera. Se aprovecha para dejar de duplicar el fallback de `fechaFin` (ahora `periodsOfResident`, deuda de la retro de la Fase 6.1) y para unificar en `aplica()` la invalidación del resultado de validación que hacía `cicla()`. Revisadas las demás pantallas: `Generator.jsx` y `DatosServicio.jsx` ya derivaban por fecha, y `Home.jsx`/`App.jsx` usan hoy **a propósito** (son la plantilla de hoy y la identidad del que entra). **Verificado en vivo** contra `dev-server.mjs`, junio-2027: Carlos/Elena/Iván pasan a R4/R3/R2 (antes R3/R2/R1), Ana y Bea salen como «FIN» con su G y su V visibles, pulsar una celda vacía suya no hace nada, pulsar la escrita la borra y la fila se va sola, y el resto de la rejilla sigue ciclando códigos igual
- [x] `v2/domain/responsible.js` — `eligibleCandidates`, `resolveMethod`, `drawResponsible`, `validateResponsible`: INV-14 (Responsable R3, sorteo reproducible), 19 tests — Fase 5, backend (acciones `estadoResponsable`/`ofrecerseResponsable`/`retirarVoluntariadoResponsable`/`ejecutarSorteoResponsable`/`listResponsables`, tabla `voluntariosResponsable`) y cliente (`Responsable.jsx`) probados E2E en navegador real
- [x] `v2/domain/accumulate.js` — `accumulatedTally`: contaje acumulado por residente en SU año de residencia en curso (§4, decisión V-9/Fase 6.1), respeta el lookahead de doblete (C-1); usa `periodOn` (nuevo en `residents.js`, misma semántica que `levelOn` pero devuelve el periodo completo). Backend: acción `listAsignacionesRango` (rango de fechas ISO, cruza meses/años). Cliente: `Generator.jsx` inyecta contaje acumulado y bloqueos reales en el prompt portátil (antes decía "que conozcas por el contexto de esta conversación" sin datos); `comprobar()` pasa por fin `bloqueos` a `validateMonth` (antes INV-5/6/7 nunca se comprobaban desde el Generador). Bug real preexistente desde la Fase 4 encontrado al diseñar la 6.1 (no en el propio código nuevo): `Calendar.jsx` tampoco cumplía el contrato C-2 (INV-7 solo veía las asignaciones del mes validado, nunca las de un mes anterior de una rotación en curso) — corregido junto con el mismo fix en el Generador. Cerrada con una puerta de consistencia de 4 agentes (normativa, referencias colgantes, cobertura de tests, UI) que encontró y corrigió 4 fallos reales antes de cerrar: `Generator.jsx` duplicaba las asignaciones del día 1-2 del mes en `comprobar()` (el histórico se pide con lookahead hasta 2 días dentro del mes nuevo para el doblete de borde, pero se reusaba sin recortar); el `desde` del histórico en `Generator.jsx` se calculaba por aniversario del residente en vez de por la fecha real del bloqueo ROTACION (podía no alcanzar lo bastante atrás); un fallo de red al cargar bloqueos/histórico en el Generador dejaba la pantalla operativa en silencio (INV-5 podía no comprobarse nunca sin avisar); y una condición de carrera en `Calendar.jsx` si se cambiaba de mes a mitad de una validación en vuelo (ahora ◀/▶ se deshabilitan mientras `validando`)
- [x] `v2/domain/cuadrante.js` — `canValidate`/`canPublish`/`canUnpublish`/`canEdit`/`stateAfterEdit`: ciclo de estados BORRADOR→VALIDADO→PUBLICADO (§2, decisión V-10, Fase 6.2), 9 tests. `v2/domain/validate.js` gana `buildMonthContext` (ensambla el ctx de `validateMonth` — una sola vez, reusada por Calendar.jsx/Generator.jsx/router.js en vez de reimplementar la misma forma de objeto tres veces). Backend: tabla `cuadrantes` (append-only, `readLatest` por mes|anio, sin fila = BORRADOR implícito), acciones `estadoCuadrante`/`marcarValidado`/`publicarCuadrante`/`despublicarCuadrante` (las 3 últimas solo Responsable en mandato; `marcarValidado` **revalida en el servidor** contra el store, nunca confía en el `violaciones` que calculó el cliente), `guardarAsignaciones` ahora bloquea ediciones de un mes PUBLICADO y revierte VALIDADO→BORRADOR al guardar. Cliente: `Calendar.jsx` muestra el estado (badge + botones Validar/Publicar/Despublicar, solo Responsable) y bloquea la rejilla si PUBLICADO; `Generator.jsx` bloquea "Aplicar" igual. 30 tests nuevos (9 dominio + 17 router + 1 api.js + 3 buildMonthContext). Cerrada con una puerta de consistencia de 8 agentes en paralelo (línea a línea, comportamiento eliminado, cross-file, reuse, simplificación, eficiencia, altitud, convenciones) que encontró y corrigió: nombres de función en español violando S-7 (`puedeValidar`→`canValidate` etc.); `Calendar.jsx`/`Generator.jsx` reimplementaban inline la misma regla que ya vivía en el dominio nuevo (ahora la importan); `router.js` reconstruía a mano el ctx de `validateMonth` tres veces (ahora `buildMonthContext` compartido) y repetía el guard de rol/mes-anio/escritura de transición en 3-4 sitios (extraído a `requireResponsable`/`validCuadranteMesAnio`/`writeCuadranteEstado`); `guardarAsignaciones` partía la fecha a mano (`slice`+`split`) en vez de `parseISO`, lo que dejaba pasar fechas imposibles sin aviso claro; un fallo de red al comprobar `estadoCuadrante` se asumía silenciosamente como BORRADOR en ambas pantallas (mismo patrón de bug que ya se había corregido en 6.1, reintroducido aquí — ahora bloquea la edición y avisa); en `Calendar.jsx`, el botón Publicar no se deshabilitaba mientras había una validación en vuelo (carrera con `marcarValidado`), y `cicla()` (editar celda) no se bloqueaba durante un guardado en curso (podía perder una edición en silencio); validar con cambios sin guardar podía disparar `marcarValidado` sobre datos del servidor distintos de lo que mostraba la pantalla (ahora se omite si hay cambios pendientes). Evaluado y NO corregido (mismo criterio que Fase 6.1): la relectura de `asignaciones`/`bloqueos` en `marcarValidado` duplica una lectura que el cliente ya hizo en una petición separada momentos antes — inherente a que el servidor revalida por su cuenta y nunca confía en el cliente, magnitud irrelevante frente a la latencia de Apps Script/Sheets a esta escala (~15 usuarios)
- [x] `v2/domain/projection.js` — proyección a Sheets legible (Fase 7.1, decisión V-11): `buildMonthSheetRows` (pestaña "YYYY-MM": código día a día como valor + totales G/GF/GP/3P/Total/Fines de Semana/Dobletes V-D como fórmulas COUNTIF/SUMPRODUCT locales a la propia fila, mismo idioma que "Cuadrante Anual" del .xlsm legado) y `buildResumenRows` (hoja "Resumen": SUMIF encadenado cruzando TODAS las pestañas mensuales publicadas por nombre de hoja + MAXIFS/MINIFS de equidad por cohorte de ingreso), 23 tests. El doblete V-D solo empareja viernes/domingo DENTRO del propio mes (nunca ve la pestaña siguiente) — misma limitación que ya tenía el .xlsm, aceptada en S-5/§4, no corregida aquí a propósito. Backend: `router.js`, acción `publicarCuadrante` ahora proyecta de verdad (`store.rebuildSheet`, ya implementado en Fase 2 pero sin invocador hasta esta fase) ANTES de escribir el estado PUBLICADO — un fallo de la API de Sheets deja el cuadrante intacto en VALIDADO, nunca un PUBLICADO fantasma; `despublicarCuadrante` no toca ninguna pestaña (V-11b). 7 tests nuevos de router (proyección real con valor exacto de celda, asignación de otro mes no se filtra, acumulación de Resumen a través de 2 meses, fallo de Sheets no persiste el estado, fallo específico de Resumen tras mensual ya escrito, reintento tras fallo sana del todo). `build/build-gas.mjs` (`DOMAIN_MODULES`) y `server/Code.gs` (`deps.domain`) actualizados; `server/domain.gs` regenerado. **Código completo, sin verificar en vivo**: no hay Sheet real hasta que llegue el acceso a la cuenta del servicio (mismo bloqueo que dejó pendiente 2.6) — la corrección de las fórmulas SUMPRODUCT/MAXIFS está probada por igualdad de texto contra fórmulas construidas a mano, no por ejecución real en Google Sheets. Contaje Trimestral (la 3ª pieza de "Resumen/Contaje" del plan) queda para una Fase 7.2 aparte (V-11d). Cerrada con una puerta de consistencia de 4 agentes en paralelo (lógica/fórmulas, integración en router, cobertura de tests, documentación) que encontró y corrigió un bug real: `buildMonthSheetRows` no excluía las guardias cedidas/compradas (`origen`) de los totales, contradiciendo `tally.js`/INV-4 (una guardia cedida contaba igual que una propia en G/GF/GP/Total/Fines de Semana/Dobletes, y ese Total inflado se arrastraba a "Resumen" por SUMIF, distorsionando también la equidad) — ahora se marca con "*" en la celda (p.ej. "G*") para que COUNTIF/SUMPRODUCT la excluyan sin cambiar las fórmulas; el 3P NO se marca (tally.js lo cuenta siempre, con independencia del origen). Además: `buildResumenRows` deduplica `publishedMonths` (defensivo, ningún invocador actual lo dispara). Limitaciones evaluadas y NO corregidas (documentadas en el código): el SUMIF de "Resumen" cruza por nombre de residente, no por id — dos homónimos activos el mismo mes mezclarían sus totales, mismo riesgo que ya tenía el .xlsm legado; si `rebuildSheet` falla en "Resumen" tras haber tenido éxito en la pestaña mensual, esta queda con un adelanto visible mientras el cuadrante sigue en VALIDADO (nunca PUBLICADO) hasta el siguiente "Publicar" con éxito, que repara ambas pestañas — ventana estrecha, de bajo impacto frente a la complejidad de una transacción conjunta a esta escala (~15 usuarios)

- [x] `v2/domain/equity.js` — cierre TRIMESTRAL de equidad (INV-3 trimestral, Fase 7.2 / P-8, decisión V-13): `validateQuarterClose` (solo el eje `total`, severidad aviso, ventana del trimestre que cierra el mes validado, descuento proporcional de BAJA de la nota [a], residentes con <½ trimestre disponible fuera de la comparación) y `quarterCloseWindow`; `v2/domain/calendar.js` gana `trimesterWindow` (definición única de los meses de cada trimestre, compartida con `trimesterOf`). Además, el cierre ANUAL pasa de código muerto a comportamiento: `yearCloseHistoryStart` (qué histórico hace falta, simétrico de `rotationHistoryStart`/C-2) y `buildYearCloseContext` (ensambla el ctx de `validateResidencyYearClose` con `accumulatedTally`, traduciendo `finde`→`findes` en un solo sitio y pasando el mes como lookahead de doblete sin sumarlo dos veces, C-1). Backend: `marcarValidado` comprueba ahora el mes **y** los dos cierres con una sola lectura del store (`monthSnapshot`), acción nueva `listBloqueosRango` (las bajas del trimestre/año completo, no solo las del mes), `bloqueosInRange` extraído. Cliente: `client/lib/closes.js` (módulo `.js` real y testeable —un `.jsx` no puede importar otro— que decide si el mes cierra algo, pide solo el rango necesario y nunca da por comprobado un cierre que falló al cargar) usado por `Calendar.jsx`; el rechazo del servidor en `marcarValidado` ahora se avisa en pantalla en vez de dejar el cuadrante en BORRADOR sin explicación. `accumulate.js` entra en `DOMAIN_MODULES` (el servidor necesita el contaje acumulado) y en `deps.domain` de `Code.gs`; `server/dev-server.mjs` recupera además `buildMonthSheetRows`/`buildResumenRows`, que le faltaban desde la Fase 7.1 (Publicar fallaba en local). 34 tests nuevos (20 dominio + 8 `closes.js` + 6 router) y la paridad del bundle ampliada a las 5 funciones nuevas. **Sin verificar en vivo contra el Sheet real**, como el resto de la Fase 7

- [x] `server/src/sheets-schema.js` — nuevo tipo de columna `"date"` (bug real de primer uso en vivo, 2026-07-21, tras conceder acceso a la cuenta del servicio): Google Sheets detecta un string `"YYYY-MM-DD"` y convierte la celda a tipo Fecha interno; `SpreadsheetApp.getValues()` devuelve entonces un `Date` real, no el string original, y `deserialize` hacía `String(fecha)` esperando ya-texto — producía basura tipo `"Tue May 07 2024 00:00:00 GMT+0200..."` que rompía `parseISO` en cascada (pantalla en blanco). Se manifestó en la primera alta autoservicio real (residentes.fechaInicio) pero afectaba a las 8 columnas de fecha del esquema por igual — ningún test lo detectó antes porque el `ss` falso de los tests nunca simula la autoconversión de tipos de Sheets. Fix: `serialize` prefija con apóstrofe (fuerza texto plano en Sheets, invisible tras guardar); `deserialize` recupera `"YYYY-MM-DD"` tanto de un `Date` real (getters LOCALES, no UTC — Apps Script corre con el huso horario del proyecto, el mismo que ancló la medianoche de esa celda) como del string con apóstrofe (el `ss` falso no simula el despojo de Sheets). 4 tests nuevos. Columnas marcadas `"date"`: residentes/periodos.fechaInicio/fechaFin, bloqueos.desde/hasta, asignaciones.fecha, responsables.periodoInicio/periodoFin/fechaSorteo, voluntariosResponsable.periodoInicio, sorteos.fecha, cuadrantes.fecha. **Verificado en vivo el mismo día** (2026-07-21): el autor repegó `server-lib.gs`, desplegó nueva versión, y confirmó que el login/alta ya funciona correctamente contra el Sheet real — el `Date` que ya estaba corrompido se autorreparó solo al releer, sin tocar la celda a mano

- [x] **INV-1 diagnostica al residente no asignable** (decisión V-21) — punto 4 de la auditoría del 2026-07-27. Un día cubierto solo por un `FINALIZADO`/`PENDIENTE`/id desconocido producía el objeto byte a byte idéntico al del día vacío, sin `residenteId`; y 1 válido + N no asignables no decía nada de la asignación inválida, que llegaba a la pestaña publicada. Ahora una violación por asignación con la causa y su fecha frontera (`reasonNotAssignable`), y el día dice «sin nadie asignable» nombrando a quién. Las dos en `aviso`; el día sin ninguna asignación sigue siendo `error`, y un 2×Mayor no se degrada por tener al lado un no asignable. 6 tests nuevos
- [x] **`parity.test.js` cubre `responsible.js` y `sheets-store.js`** — punto 7 de la misma auditoría. Los dos módulos estaban en el bundle sin que ningún test lo exigiera: **comprobado empíricamente** que quitando `responsible` de `DOMAIN_MODULES` o `sheets-store` de `SERVER_MODULES` la suite entera seguía en verde, mientras `server/Code.gs:142` llama a `Server.makeStore` dentro de `deps_()`, o sea en CADA petición, y `eligibleCandidates` reventaría al abrir la pantalla de Responsable. Es la clase de fallo que ya pasó en producción el 2026-07-26 (`quarterCloseWindow is not a function`). `runAll` encadena ahora los cuatro exports de INV-14 como los encadena el router (sorteo → `validateResponsible`), y una prueba nueva compara `Server.makeStore` del bundle contra el ESM sobre un `ss` en memoria (cabecera automática, lote, borrado por `emptyField`, shadow-swap). Reverificado: con el test puesto, los dos experimentos SÍ rompen la suite

- [x] **Validación de entrada en el router y ausencias con fecha ilegible** (decisión V-22) — punto 6 de la auditoría del 2026-07-27, comprobado con sonda antes y después. Cierra la puerta de entrada (`validRango` único con `parseISO`; listas blancas de `codigo`/`origen` antes de escribir el lote) y arregla el diagnóstico de las filas que ya estén escritas: se apartan del contexto y se reportan con un `error` INV-5 que las nombra, sin dejar de aparecer en las listas para poder cancelarlas. Medido: con `desde="30/02/2026"` INV-5 pasaba de 31 violaciones a **0** —baja invisible— y ahora emite la que nombra la fila. 12 tests nuevos, y las cinco mutaciones que rompen el arreglo comprobadas contra la suite (quitar la inyección de violaciones, esconder la fila corrupta, no apartarla en `validar`, volver al `>` lexicográfico, quitar las listas blancas)

- [x] **Contaje Trimestral y ventana de curso en las hojas agregadas** (decisión V-25) — punto 11 de la auditoría del 2026-07-27, y la última pieza de la Fase 7 que faltaba por CÓDIGO (V-11d la dejó para una 7.2 aparte). `buildContajeTrimestralRows` en `projection.js`, tercer `rebuildSheet` en `projectCuadranteToSheets` — la ventana en la que el Sheet puede quedar a medias pasa de dos hojas a tres, sigue siendo idempotente y se sana reintentando Publicar, sin transacción. El Resumen deja de acumular de por vida y de invocar el umbral de INV-3 en un cartel que no lo mide. 20 tests nuevos, paridad del bundle ampliada a `buildContajeTrimestralRows`/`cursoLabel`, y cuatro mutaciones comprobadas contra la suite (quitar `projection` del bundle, agrupar trimestres por año natural, quitar la rectangularidad, volver a la ventana de por vida). **Pendiente de verificar en vivo contra el Sheet real**: las fórmulas están fijadas por igualdad de texto, que es exactamente el punto ciego que el punto 1 de la auditoría documentó

- [x] **Derivación única de periodos formativos** (decisión V-24) — fase 1 del punto 10 de la auditoría, y prerrequisito de poder rellenar la tabla `periodos`. Las siete copias del fallback de `fechaFin` pasan a `periodsOfResident`, y `v2/domain/test/periodos-unificados.test.js` (8 tests) fija la COHERENCIA entre módulos, que es donde vivía el fallo: prueba el mismo residente con periodos editados a través de `validate`, `projection`, `accumulate`, `equity` (los dos cierres), `thirdpost` y `responsible`, y contrasta cada resultado con el que daría la derivación nominal. Cuatro mutaciones comprobadas contra la suite; la del rango de presencia del cierre trimestral **no se detectaba** al primer intento porque el test tenía la diferencia a 1 y no llegaba a emitir — lo destapó mutar el código, no leerlo

- [x] **Periodos formativos editables y corrección de fechas** (decisión V-24, fases 2 y 3) — cierra el punto 10 de la auditoría por el lado del backend, y con él la nota [a] deja de ser inalcanzable. Dos fallos que el informe no listaba y que habrían dejado el punto sin efecto: (1) `buildMonthContext` **recortaba** el residente a `{id,fechaInicio,fechaFin}` y borraba los periodos, así que por muy bien que el router los hidratara `levelOn` volvía al aniversario nominal sin lanzar ni avisar; (2) `residentes` se leía con `readRecords` y no con `readLatest`, de modo que reinsertar la fila corregida no tenía ningún efecto visible — lo destapó el test, no la lectura del código. 13 tests de router nuevos, incluido el que cierra el punto (los periodos editados llegan a `marcarValidado` e INV-11 ve al R1 alargado en verano, bloqueando) y el del permiso del ciclo, que también deriva del nivel. Cinco mutaciones comprobadas contra la suite
- [x] **Pantalla de residentes y periodos** (`client/screens/Residentes.jsx`, enlazada desde Ajustes) — cierra la laguna que este mismo punto habría dejado abierta: sin ella la tabla `periodos` nacía vacía, igual que les pasó a `festivos` y `eventos` hasta `DatosServicio.jsx`, y el `aviso` de V-21 seguía sin salida real desde la app. Una ficha por residente ordenada por nivel de hoy (los FINALIZADO al final, nunca escondidos: su fecha de fin es justo la que suele haber que corregir), con las fechas de residencia y los cuatro periodos. Valida EN VIVO con la misma `validateTrainingPeriods` que el servidor, así que el botón no se pulsa para recibir un error que ya se podía ver. Escribir exige el permiso del ciclo (V-16) y a quien no lo tiene se le dice en vez de esconderle la pantalla. La etiqueta «periodos editados» compara con los derivados en vez de mirar si existen filas: `restaurarPeriodos` no puede borrar —la tabla es append-only y no tiene `activo`, y añadírsela es lo que V-19(d) descartó—, así que escribe los automáticos, y con la comprobación por existencia la etiqueta se quedaba mintiendo tras restaurar. **Verificado en vivo**: alargar el R1 de Elena la pasa de R2 a R1 en su propia ficha, un solape enseña «Periodo R2 se solapa con R1» y deshabilita el botón, «Volver a los automáticos» la devuelve a R2 y se deshabilita solo, y un R2 ve las diez fechas bloqueadas y sin ningún botón de guardar
- [x] **El cliente también deriva por `periodsOfResident`** (V-24, fase 4): `Home.jsx`, `Generator.jsx` (dos sitios) y `DatosServicio.jsx` derivaban el nivel con `defaultTrainingPeriods` directo, así que con la tabla rellena habrían mostrado un nivel distinto del que juzga el validador — la misma divergencia que la fase 1 cerró en el dominio. `Calendar.jsx` no hacía falta tocarlo: la rama `claude/unruffled-nobel-53f02f` ya lo pasó a `periodsOfResident` por su cuenta. **Verificado en vivo**: editar los periodos de Elena (R2 → R1 alargado) mueve su nombre de R2 a R1 en el equipo de Inicio, y en Datos del servicio el sorteo de un evento del 18-dic-2026 pasa a decir «No hay ningún R2 en esa fecha», porque era la única
- [x] **Bug de producción arreglado de paso**: `Responsable.jsx` accedía a `estado.siguiente` fuera del guard de `!estado`, y una excepción en render desmonta el árbol — la pantalla de Responsable se quedaba en blanco de forma permanente y nadie podía ofrecerse voluntario ni lanzar el sorteo (todo INV-14). Entró con V-16 y llevaba así en `main`. Comprobado que era preexistente antes de tocarlo

- [x] **Reintento de fallos de transporte en el cliente** (decisión V-26) — arregla un bug de producción que bloqueaba el LOGIN de forma intermitente: el `/exec` responde 302 a un enlace de un solo uso y su segundo salto falla a menudo, y `callBackend` traducía eso a «HTTP 404» sin reintentar ni una vez. 6 tests nuevos en `client/lib/test/api.test.js` (reintento con éxito, reintento ante excepción de red, tope de 3 intentos, las escrituras con UN solo intento, `{ok:false}` sin reintentar, backoff creciente) y cuatro mutaciones comprobadas contra la suite. **Verificado contra el `/exec` real**: 5 llamadas seguidas, todas con éxito, y una de ellas necesitó el reintento — antes, 6 peticiones seguidas daban 404 y timeouts
- [x] **Excepcion deja de estar muda** (decisión V-29) — tabla `excepciones`, acciones `listExcepciones`/`crearExcepcion`/`anularExcepcion` (permiso del ciclo, V-16) y `buildMonthContext` gana el parámetro `excepciones` que le faltaba: `validateMonth` ya sabía leer `ctx.excepciones` desde que INV-9 se implementó, pero nada en la cadena real (`buildCuadranteCtx`/`monthSnapshot`/`marcarValidado`) se lo pasaba, así que un 2×R2 justificado avisaba igual que uno sin justificar. Pantalla nueva en `DatosServicio.jsx` (que pasa a llamarse "Datos del servicio"). `Calendar.jsx` también la pide en su `validar()` local. 9 tests nuevos (1 dominio + 7 router + 1 de pass-through de `buildMonthContext`). **`Generator.jsx` NO la recibe** (ver V-29, columna motivo): construye su propio `ctx` y no le pasa `excepciones` ni `eventos`, deliberadamente sin tocar para no pisar la reescritura async de la Fase 6.1 en curso en otra rama
- [x] **`origen` CEDIDA/COMPRADA alcanzable desde la app** (decisión V-30) — cierra la rama de INV-4 que quedaba fuera desde V-22/V-11: el servidor ya validaba `origen` y `tally.js`/`projection.js` ya lo excluían del cómputo, pero ningún cliente lo escribía nunca. `Calendar.jsx` gana un segundo gesto (mantener presionado, no doble-click — ver motivo en V-30) que abre un `<select>` inline sobre una celda con G/GF/GP/3P; guardar sigue siendo el mismo botón y la misma acción `guardarAsignaciones` de siempre. **Verificado en vivo** contra `dev-server.mjs`: click simple sigue asignando código al instante sin ningún retraso perceptible; mantener presionado ~500ms abre el selector sin ciclar el código de paso; elegir "cedida" pinta "G\*" en la celda; guardar y recargar la página confirma `origen:"CEDIDA"` persistido leyendo directo la respuesta de `listAsignaciones`; y volver a hacer click normal sobre esa misma celda cicla el código y limpia `origen` (deja de verse el "\*")
- [x] **Generador de cuadrante con IA en el servidor** (decisión V-45) — sustituye a la pantalla `Generator.jsx`, que se elimina junto con su pestaña. Núcleo puro nuevo: `server/src/ai-prompt.js` (`buildGenerationPrompt`, `buildRetryPrompt`, `parseGenerationResponse`) y `server/src/ai-generator.js` (`generateSchedule`, el ciclo de ≤3 intentos con `llm` y `validar` inyectados). `v2/domain/apply.js` (`monthReplacementPlan`, subido desde `client/lib/apply-month.js`) entra en el bundle para que el servidor escriba el mes por el mismo camino que escribía el cliente. Acción `generarCuadranteIA` en el router (permiso `requireCicloPermiso`, estado `canEdit`, validación con `validateMonth`+`validateThirdPost`, bitácora en la tabla nueva `generaciones`), adaptador `llm_()` en `Code.gs` y botón condicional en `Home.jsx`. El prompt no se reescribe de cero: es el de `Generator.jsx`, portado tal cual con sus secciones de datos reales (bloqueos, festivos, puentes, voluntarios 3P, eventos, preferencias) y una norma 13 nueva para INV-15, que no existía cuando se escribió
- [x] **Modo «completar» del generador con IA** (decisión V-47) — `v2/domain/apply.js:monthCompletionPlan` (8 tests), sección «GUARDIAS YA FIJADAS» y norma 14 en `server/src/ai-prompt.js` (2 tests), `modo` en `generarCuadranteIA` con validación del mes resultante y `FORMATO` por fijada pisada (6 tests de router, uno de ellos reencuadrado: el reemplazo pasa a exigir `modo: "reemplazar"`), selector de modo en la tarjeta de Inicio, fila propia marcada (★) y pista en el cuadrante, tarjeta de acceso en Preferencias, y columna `modo` en `generaciones`
- [x] **Arranque más rápido** (2026-09-04, pedido del autor: «más velocidad al iniciar, sobre todo al meter el correo con Google») — `client/loader.js` cachea en `localStorage` el JSX transpilado (clave: huella SHA-256 del fuente; un despliegue invalida sola la caché) e inyecta Babel Standalone (~2,3 MB) solo si algún fichero no está en caché, en vez del `<script>` bloqueante de `index.html`; el nonce del login se pide en la primera línea del arranque (`auth.js:prefetchNonce`) y `Login.jsx` lo consume, espera a que el script de Google esté cargado (antes, si llegaba tarde, el botón no aparecía nunca), refresca el nonce cada 4 min y al volver a la pestaña (caduca a los 5 en el servidor) y rehace el login al volver del formulario de alta; `login`/`altaResidente` devuelven la lista de residentes y `App.jsx` se ahorra la petición de `listResidentes` al entrar; `Home.jsx` deja de pedir dos veces `estadoResponsable`/`estadoCuadrante`; `Calendar.jsx` pide las asignaciones del mes en UNA petición (el rango ya contenía el mes); y una sesión rechazada por el servidor (12 h) cierra la app y vuelve al login con aviso (`api.js:onSessionInvalid`) en vez de dejar cada pantalla en «sesión expirada» sin salida. La fuente de Google deja de bloquear el pintado. 9 tests de `auth.js`, 4 de `api.js`, 3 de router
- [x] **Revisión adversarial de 2026-09-04** (9 agentes por dimensión + 2 verificadores por hallazgo, sobre un worktree limpio) — corregido: `handleAlta` valida fechas y nombre (una fecha no ISO en `residentes` dejaba en blanco todas las pantallas para todo el equipo; el cliente además aparta ahora a los residentes ilegibles con `client/lib/residentes.js` y `Residentes.jsx` los enseña con su error y el editor de fechas); `guardarPreferencias` valida campo a campo (`validPrefs`: un `fechasEvitar` que no era lista rompía el generador del mes entero); `guardarAsignaciones` exige un `residenteId` existente y un `puesto` del enum; el email se compara con `trim()`; las lecturas por mes rechazan `mes`/`anio` no numéricos; las anulaciones son idempotentes (no apilan una segunda fila inactiva); `crearEvento` deduplica voluntarios; el sorteo del Responsable se decide como pronto el año anterior; **comprobar-y-escribir bajo el lock** (`sheets-store.js:transaction`; la reentrada la absorbe el propio store, así que `Code.gs` no cambia y un despliegue que repegue solo `server-lib.gs` no puede dejar el lock colgado): `marcarValidado`, `publicarCuadrante` y `ejecutarSorteoResponsable` van enteros dentro, y `generarCuadranteIA` relee el mes al escribir y responde `CONFLICTO` sin escribir si cambió mientras el modelo pensaba (no se tiene el lock durante el minuto de generación: bloquearía todos los guardados). `estadoCuadrante` anuncia `modosGeneracion` y el cliente solo ofrece «completar» si el servidor desplegado lo entiende: el cliente sale a Pages con el merge y el servidor cuando alguien lo redespliega, y un servidor anterior a V-47 ignoraría el modo y reemplazaría el mes; `Calendar.jsx` no se queda con `validando` colgado ante una excepción, pide bloqueos/festivos/excepciones en paralelo al validar, no cuenta las celdas vaciadas como asignaciones, enseña las violaciones con las que el servidor rechazó `marcarValidado`, bloquea la rejilla si no pudo cargar el mes, pregunta antes de perder celdas sin guardar (cambio de mes, de pestaña o cierre de sesión) y dice cuándo un mes sin errores no pasa a VALIDADO por tener cambios pendientes; `Prefs.jsx` no se cae con una ausencia de fecha ilegible (la enseña con su texto y su botón de cancelar) ni pinta los bloqueos de un mes anterior que lleguen tarde; el prompt del generador conserva la preferencia explícita de 0 guardias. 11 tests nuevos de router (`router-atomico.test.js`) + 2 de `client/lib/residentes.js`. `Code.gs` no cambia
- [x] **Bordes del dominio destapados por la misma revisión** (2026-09-04) — (1) **INV-15 en el borde del mes también en el servidor**: `buildCuadranteCtx` metía histórico solo con rotación cercana (C-2), así que una guardia el día 1 pegada a la del último día del mes anterior pasaba `marcarValidado` y el generador con IA la escribía; ahora el histórico lleva siempre la víspera y el día siguiente al mes. (2) **Una sola ventana de cierre** (`residents.js:closingPeriodOn`): `thirdpost.js` la calculaba sobre el aniversario nominal y `equity.js` sobre `periodsOfResident`, y con periodos editados INV-8c e INV-3 cerraban el año en meses distintos; además quien dejó la residencia antes del fin nominal ya no «cierra» los años que no cursó (salían ceros contra su cohorte dos mayos seguidos), y una baja que cubre el año entero se excluye de la comparación (`availabilityFraction` devolvía 1 para el caso degenerado). (3) **INV-6 cuenta residentes, no filas**: unas vacaciones dentro de una rotación eran dos ausentes. (4) **INV-2 exime la presencia parcial** (el R1 que entra el 27 de mayo, quien termina a mitad de mes). (5) **La cola de Imaginaria aparta a quien tiene una ausencia ese día** (`imaginariaQueue` gana `bloqueos`; antes proponía llamar primero a quien llevaba un mes de baja). (6) **P-13 SOBRECARGA por mes**: dividía los días del periodo entero entre los disponibles y lo comparaba con «6/mes», así que cualquier ausencia de más de cinco semanas avisaba (una rotación de tres meses con seis Mayores libres daba 15,3); ahora `min(días, 30,44) / disponibles`. (7) `crearEvento` rechaza un segundo evento activo del mismo tipo en el mismo curso (`shapeEventos` se quedaba con el último en silencio); `listBloqueosRango` devuelve solo filas legibles (los cierres hacen aritmética de fechas); `Calendar.jsx` aparta las ausencias ilegibles antes de validar con el mismo error INV-5 que el servidor (`client/lib/bloqueos.js`). **Pendiente de decisión** (no tocado): compañeros de la misma cohorte cuyo año de residencia cierra en meses distintos (periodos editados o `fechaInicio` distinta dentro del mismo año) nunca se comparan en el cierre anual — es un hueco del modelo de INV-3, no un fallo de código, y cambiarlo es una decisión de negocio
- [x] **Segunda tanda de la revisión adversarial** (2026-09-04, las cuatro dimensiones que quedaron sin correr: generador, almacén/esquema, pantallas secundarias, navegador) — **Almacén:** todo texto libre se escribe con apóstrofe (`sheets-schema.js:serialize`, como ya hacía el tipo `date`): `setValues` ejecuta como fórmula cualquier celda que empiece por «=» y autoconvierte lo que parezca fecha o booleano (una nota «15/7» volvía como «Thu Jul 15 2027…», «TRUE» como «true», y un nombre «=HYPERLINK(...)» quedaba como fórmula en una tabla que no se borra); `handleAlta` rechaza además un nombre que empiece por «=» porque la proyección publicada escribe texto y fórmulas mezclados y no puede prefijarlo todo; `rowsToRecords` descarta las filas con el contenido borrado a mano (Supr en vez de «eliminar fila»), que se convertían en un registro `{}` sin id que tumbaba validar, generar y la imaginaria para todo el equipo sin decir qué fila; el `build-gas.mjs` falla alto ante `export let` y `export const A = 1, B = 2` (antes los transformaba mal en silencio) y `parity.test.js` exige que el bundle exporte exactamente los nombres de los módulos ESM. **Comprobar-y-escribir bajo el lock también en los otros escritores:** `guardarAsignaciones` leía el estado FUERA del lock y escribía dentro —una celda que esperaba mientras otro validaba se colaba en un mes ya VALIDADO sin devolverlo a BORRADOR—; ahora estado y escritura van juntos, igual que la simulación P-13 de `crearBloqueo` (dos vacaciones simultáneas de los dos únicos Pequeños pasaban ambas), la unicidad de `handleAlta` (dos altas del mismo email) y la de `crearEvento`. **Generador (V-45/V-47):** una clave `fecha|residente` repetida en la propuesta (G y 3P) es `FORMATO` —el validador juzgaba una lista y el Sheet, con «última fila gana», guardaba otra: el día perdía su Mayor—; una guardia propuesta sobre una celda V/R/B también es `FORMATO` y las celdas marcadas van al prompt («CELDAS YA MARCADAS»), porque la tarjeta promete conservarlas en los dos modos; el plan reconoce solo a los residentes R1–R4 del mes (los mismos que lista el prompt), así que un FINALIZADO propuesto cae en `desconocidos` en vez de escribirse con un aviso; el prompt lleva las guardias de los BORDES del mes (último día del anterior, día 1 del siguiente) para que la norma 13 sea cumplible en el primer intento; en `completar`, si las fijadas ya incumplen por sí solas una regla dura (INV-15/5/11) se responde `FIJADAS_INVALIDAS` sin llamar al modelo ni gastar intentos, con la causa real (INV-1 se excluye: los huecos son lo que va a rellenar); **la propuesta se vuelve a juzgar bajo el lock con un snapshot fresco** antes de escribir —una BAJA registrada mientras el modelo pensaba no cambiaba la huella del mes y se escribían guardias sobre ella— y si ya no pasa es `CONFLICTO`; la bitácora se acota a 50 violaciones (una celda de Sheets admite 50.000 caracteres) y su fallo nunca convierte un mes ya escrito en `ok:false`. **Pantallas:** `client/lib/fechas.js` (`fechaValida`/`rangoValido`): teclear el año en cualquier `<input type="date">` emite «0002-09-04» y `parseISO` lanzaba EN EL RENDER, desmontando la app entera en el alta, en el formulario de ausencias, en los periodos y en los eventos (con el texto de festivos pegado incluido); `partirResidentesLegibles` comprueba también `fechaInicio`/`fechaFin` (un residente con periodos editados y `fechaFin` ilegible pasaba por legible en todas partes menos en la única pantalla que la corrige); `App.setTab` ignora la pestaña ya activa (preguntaba «¿salir y perderlos?» sin salir y luego dejaba de preguntar); el permiso del ciclo en pantalla sigue a `estadoCuadrante.responsableId` (releído del store) y no solo al `rol` del token, así que el ganador del sorteo tiene sus botones sin volver a entrar; `Responsable.jsx` y `Festivos` descartan la respuesta de un año que llega tarde; `Residentes.jsx` usa la lista del contexto (pedía `listResidentes` dos veces); `Login.jsx` ofrece «Reintentar» si no hubo nonce o no cargó el script de Google; la tarjeta del generador distingue «comprobando» de «falló la consulta» (con reintento) y sobrevive a un cambio de pestaña mientras genera (recuerda la generación en curso y su resultado por mes); `auth.js` liga cada nonce a su callback (el refresco de los 4 min pisaba el nonce de un login con el selector de cuentas de Google abierto). **De la verificación posterior (E2E en navegador + un verificador por hallazgo sobre el HEAD corregido):** en «reemplazar» se juzga también lo que SOBREVIVE (los 3P que la propuesta no borra por V-38) y se le cuentan al modelo como fijados — antes se escribía un mes con INV-15 y la tarjeta decía «generado y guardado»; quien termina o empieza la residencia a mitad de mes lleva su «solo hasta/desde» en el prompt y una guardia fuera de esos días es `FORMATO` (INV-1 solo la avisaba, V-21); el plan se recalcula con el snapshot fresco en el segundo juicio; `sortearEvento` comprueba y escribe bajo el lock; una nota suelta a la derecha de la tabla ya no resucita una fila vacía; el bundler sigue la sentencia `export const` a través de varias líneas; `Calendar.jsx` no deja un cambio pendiente cuando una celda vuelve a su valor guardado (un clic + Deshacer en un mes VALIDADO escribía una fila vacía y lo devolvía a BORRADOR); la ficha de `Residentes.jsx` no pisa lo tecleado cuando `listResidentes` responde tarde; la tarjeta del generador se enseña inerte a cualquier Mayor mientras carga (no solo al Responsable del token); «Cancelar» una ausencia se deshabilita mientras la petición está en vuelo. 27 tests nuevos de servidor/dominio/bundler + 4 de `client/lib`. **Cierre tras el merge (2026-09-05, verificación de los 13 hallazgos restantes + revisión por lentes del diff completo):** un `useState` añadido en `Prefs.jsx` quedó tras el `return` temprano y desmontaba la app (React #310) a quien entraba en Preferencias antes de que llegara la lista de residentes — hotfix #48; INV-2 volvía a medir la «actividad» sobre el histórico ampliado y avisaba «0 < mínimo 4» a quien tuvo guardias el mes anterior y ninguna en el mes (rotación externa): ahora solo cuenta las del mes; el generador reconoce como residente del mes a quien se incorpora a mitad (nivel R1–R4 el día 1 O el último día, con su «solo desde» en el prompt), así que sus fijadas dejan de ser de un «desconocido»; en «reemplazar», una G propuesta sobre un 3P que se iba a conservar es `FORMATO` en vez de pisarlo en silencio y contarlo como respetado; una fila repetida idéntica ya no gasta intentos (solo cuenta como duplicada si los códigos difieren); `auth.js` vacía el contenedor antes de repintar el botón de Google (el `renderButton` real añade, no sustituye); `Prefs.jsx` sanea `fechasEvitar` al mes (una fecha legada de otro mes impedía guardar sin que se viera) y parte de cero al cambiar de mes; `index.html` versiona la URL de `loader.js` (un `index.html` nuevo con el `loader.js` viejo cacheado por Pages dejaba «No se pudo cargar el compilador»); la tarjeta del generador se enseña inerte también al acceso de desarrollador (V-46) mientras carga; y `Calendar.jsx` ya no dice «está publicado» cuando lo que hay es un fallo de carga. El comentario del nonce por callback en `auth.js` se corrige: el servidor verifica el nonce del token, no el del cuerpo, así que aquel cambio es coherencia y no un arreglo. **No tocado** (impuro, sin test posible y fuera del despliegue de hoy, que no cambia `Code.gs`): `fetchTokeninfo_` reintenta tres veces ante un HTTP 400 de tokeninfo (token caducado), que no es transitorio — 1,2 s y un mensaje que culpa a Google donde tocaría «vuelve a iniciar sesión»

**Núcleo de dominio (invariantes de mes y de cierre): 151 tests en verde, cero dependencias, cero I/O.**
Lagunas conocidas y aplazadas con su fase, tras la puerta de consistencia (§5: INV-12/13). Fuera
del núcleo puro (proceso, Fase 2+): comunicación trimestral a tutoría y envío del cuadrante a
Coordinación (Raquel); responsabilidad del busca del Pequeño; distribución de tareas intra-guardia.

## 8. Registro de propuestas

Cola única de reglas propuestas y **no** vigentes. Existe porque los ocho documentos de `docs/`
(julio 2026, autoría de Agustín) proponen reglas de negocio sin citar ningún `INV-n`, de modo que
nadie podía saber si una propuesta corregía un invariante, lo ampliaba o lo desconocía. Un documento
de `docs/` **no introduce una regla**: propone una fila aquí, y solo al pasar a `aceptado` se toca
§5 o el dominio.

Estados: `propuesto` (sin decidir) · `aceptado` (decidido, sin implementar) · `implementado` ·
`rechazado` (decidido en contra; se registra para que no vuelva a proponerse sin datos nuevos).

| # | Propuesta | Origen | Toca | Respaldo en `normativa.pdf` | Estado |
|---|---|---|---|---|---|
| P-1 | Medir la equidad con un **índice de puntos ponderados** (pesos por tipo de día, decimales) en lugar de contadores enteros | `EQUITY_SYSTEM.md`, `reglas-equidad-descanso.md`, `impacto-descanso-pesos-propuestos.md` | INV-3, INV-4 | **En contra.** p.1 y p.2 fijan «diferencia máxima de 1» sobre cada eje por separado, y el umbral 1 solo tiene sentido sobre guardias enteras | `rechazado` |
| P-2 | Tratar **vacaciones y rotación como bloqueo DURO** que impide asignar guardia | `casos-de-prueba-validador.md` (D1), `paquete-generacion-prompt.md` | INV-5, V-8 | **Ausente.** p.2 hace la rotación «prioritaria sobre las vacaciones» pero nunca prohíbe asignar guardia en esos días | `rechazado` |
| P-3 | Redefinir la **«mochila» del R1** como un tercero que cubre al R1 | `reglas-equidad-descanso.md`, `EQUITY_SYSTEM.md`, `GUARD_COMPOSITION_RULES.md`, `paquete-generacion-prompt.md` | INV-8, INV-1 | **En contra.** p.3 es literal: «Los Residentes que hagan tercer puesto siempre deben cubrir primero los días que haya un R1» | `rechazado` |
| P-4 | Comparar la equidad **fuera de la cohorte** (R4 con R3) | `EQUITY_SYSTEM.md` (§4), `reglas-equidad-descanso.md` | INV-3, V-2 | **En contra.** «del mismo año formativo» / «del mismo año» aparece cinco veces (p.1, p.2, p.3, p.4) | `rechazado` |
| P-5 | Mochila del R1 activa de **junio a diciembre** | `EQUITY_SYSTEM.md`, `GUARD_COMPOSITION_RULES.md`, `paquete-generacion-prompt.md` | INV-11, INV-2 | **En contra.** p.1: «En junio, julio y agosto, dado que se organizan sin los R1» | `rechazado` |
| P-6 | Leer INV-7 como **viernes _o_ sábado** en vez de viernes _y_ sábado | `casos-de-prueba-validador.md` (D3) | INV-7 | **Ambiguo en la fuente.** p.2 dice «al menos una de las guardias de viernes y sábado», que admite ambas lecturas | `implementado` |
| P-7 | **Un cuadrante independiente por grupo** (Mayores y Pequeños) en vez de uno único mensual | `casos-de-prueba-validador.md` (G3/CO2) | INV-1, V-9, V-10, V-11 | **Solo en apariencia.** p.1 («Los residentes se dividen en 2 grupos […] Cada grupo elaborará un cuadrante provisional») describe la composición de cada guardia y la organización de cada grupo, no dos unidades de datos — ver decisión V-15 | `rechazado` |
| P-8 | Comprobar la equidad también **al cierre de cada trimestre**, no solo del año de residencia | `EQUITY_SYSTEM.md`; ya previsto como Fase 7.2 | INV-3 | **A favor.** p.2: «una vez cerrado el cuadrante trimestral, la diferencia de número de guardias entre residentes del mismo año no supere 1» | `implementado` |
| P-9 | Catálogo cerrado de **tipos de guardia y horarios** (`TipoGuardia`, retribución, tramos) | `GUARD_SHIFT_TIME_RULES.md` | T-1, S-4, INV-12 | **Ausente.** La normativa no menciona horarios, tramos ni retribución en ninguna de sus 4 páginas: extensión sin respaldo normativo, decisión libre del autor | `rechazado` |
| P-10 | Vacación solicitada en fechas de evento (Navidad/despedida): el visto bueno se reduce a que la guardia siga cuadrando entre compañeros del mismo grupo (converge con P-13); tutoría y Jefe de Servicio no se modelan (decisión del autor, 2026-08-07) | `propuesta-visto-bueno-vacaciones-eventos.md` (lectura directa de `normativa.pdf`, sin documento previo) | Sin invariante propio — reutiliza el mecanismo de P-13 | **A favor en la letra** (p.3, «Eventos del servicio»: exige visto bueno de compañeros de guardia, tutoría y Jefe de Servicio), **pero el autor decide no exigir digitalmente las dos partes externas a la app** — mismo criterio estructural que V-14 | `implementado` (sin código propio: satisfecho por el mecanismo de P-13, que se aplica a cualquier VACACIONES/ROTACION, incluidas las fechas de evento) |
| P-11 | Modelo de datos concreto para **Imaginaria** (INV-13): entidad `ImaginariaCobertura` append-only, cola derivada por última cobertura, exclusión de R1 y de quien tenga guardia el día anterior/siguiente a la incidencia, sin exigencia de equidad | `imaginaria-modelo-propuesto.md` (lectura de `normativa.pdf` + respuestas del autor sobre la práctica real, no escritas en la normativa) | INV-13 | **Parcial.** p.4 («-Imaginaria») funda las dos listas, el criterio "lista del grupo de la incidencia" y que cesión/compra no descuenta de la imaginaria; el orden de la lista, la exclusión de R1, la exclusión por proximidad de fecha y la ausencia de exigencia de equidad **no están en la normativa** — son respuestas del autor sobre la práctica real, marcadas como tal en el documento | `implementado` (decisión V-20; se implementa como HERRAMIENTA, sin validación a posteriori, y las tres reglas sin respaldo quedan declaradas en §5.1) |
| P-12 | **División de vacaciones entre Navidad (23–26 dic) y Año Nuevo (29 dic–6 ene)**: por residente individual, si tiene ausencia (VACACIONES/ROTACION) en las dos ventanas a la vez, avisa — converge con P-13, cuarto riesgo de `blockPreview.js` | `propuesta-division-navidad-anio-nuevo.md` (conocimiento de práctica real del autor, no está en `normativa.pdf`) | Sin invariante propio — reutiliza el mecanismo de P-13 | **Ausente.** La normativa no menciona reparto de vacaciones alrededor de Navidad/Año Nuevo en ninguna de sus 4 páginas — extensión sin respaldo normativo, práctica real del servicio | `implementado` (`DIVISION_NAVIDAD_ANIO_NUEVO` en `v2/domain/blockPreview.js`; 7 tests nuevos, 629/629 en verde) |
| P-13 | **Simulación preventiva de cobertura** al registrar un Bloqueo (vacaciones/rotación): riesgo de imposibilidad (espejo INV-1, bloquea solo dentro de 3 meses), riesgo de sobrecarga (espejo INV-2, aviso; desde 2026-09-04 medido por mes: `min(días del periodo, 30,44) / disponibles en el peor día` contra el techo de 6, no los días del periodo entero) y riesgo de concentración por nivel (nuevo, más de 2 del mismo R1–R4 a la vez, aviso) | `propuesta-simulacion-preventiva-cobertura.md` (conocimiento de práctica real del autor, no está en `normativa.pdf`) | INV-1, INV-2 (umbrales espejados) — el eje de concentración por nivel no tiene invariante previo | **Ausente.** p.4 solo dice, en general, que la organización de vacaciones "debe realizarse en consonancia con el resto de grupo de guardias", sin fijar ningún umbral — extensión sin respaldo normativo, práctica real del servicio | `implementado` (`v2/domain/blockPreview.js`, cableado en `crearBloqueo`; 10 tests nuevos, 622/622 en verde) |

### 8.1 Las trece ya cerradas

**P-12, aceptada el 2026-08-08 por decisión directa del autor, implementada el
mismo día** (`DIVISION_NAVIDAD_ANIO_NUEVO` en `v2/domain/blockPreview.js`; 7
tests nuevos, 629/629 en verde). Cierra las tres preguntas abiertas: **ventanas**
—Navidad 23–26 dic, Año Nuevo 29 dic–6 ene, estirada hasta Reyes porque
también es día de guardia especial (retribución doble, fuera de la app
por P-9)—; **alcance** —por residente individual contra sí mismo, no
por grupo, la composición diaria de INV-1 no cambia—; **severidad**
—`aviso`, confirmado—. Reutiliza el mismo conjunto de motivos que INV-6
(`AUSENCIA_SIMULTANEA`: VACACIONES + ROTACION, BAJA fuera por
impredecible) y converge con P-13 en vez de ser un mecanismo aparte: se
añade como cuarto riesgo de `previewBloqueoRisk`, mismo momento y misma
pantalla que los otros tres.

**P-13, aceptada el 2026-08-07 por decisión directa del autor, implementada el
mismo día** (`v2/domain/blockPreview.js`, cableada en `crearBloqueo` de
`server/src/router.js`; 10 tests nuevos en `blockPreview.test.js`, 622/622 en
verde). Cierra las tres preguntas que la propuesta
dejaba abiertas: (a) compara siempre contra los `Bloqueo` activos ya
existentes del mismo grupo — sin eso la simulación no dice nada; (b) el
riesgo de imposibilidad (espejo INV-1) bloquea, pero **solo si el
periodo cae dentro de los 3 meses** que ya cubre la sesión de armado
del cuadrante — más allá de esa ventana solo avisa, porque las
vacaciones a veces se piden con mucha más antelación que esa sesión, y
bloquear un pedido ya hablado con tutoría con un año de antelación
dejaría a alguien sin poder registrarlo aunque las cosas puedan cambiar
antes. Se añade un tercer eje no previsto en la propuesta original:
**riesgo de concentración por nivel** — más de 2 residentes del mismo
`nivel` (R1, R2, R3 o R4; no grupo, no cohorte) ausentes a la vez,
siempre `aviso`, nunca bloquea, porque el grupo en conjunto puede seguir
teniendo cobertura. Ese eje agrupa por `nivel`, no por `cohorte` —
`CLAUDE.md` ya marca esas dos nociones como fáciles de confundir, y
aquí el riesgo es real porque INV-6 ya tiene su propio "máx. 2
simultáneos" pero agrupado por cohorte y solo sobre rotación; ambas
reglas se ven iguales sin serlo. BAJA queda fuera de los tres cálculos
por impredecible — ya la cubren INV-5/INV-6 aparte.

**P-10, aceptada el 2026-08-07 por decisión directa del autor, satisfecha por
la implementación de P-13 el mismo día** (sin código propio: el mecanismo de
P-13 se aplica a cualquier `Bloqueo` de VACACIONES/ROTACION, no solo a los de
las fechas de evento). La normativa (p.3) exige visto bueno de
compañeros de guardia, tutoría **y** Jefe de Servicio para vacaciones
en fechas de evento; el autor decide no exigir digitalmente el de las
dos partes que no usan la app, por el mismo motivo estructural de V-14
("la app sobrevive sin administrador"): bloquear con un visto bueno que
nadie puede dar dentro de la herramienta deja la vacación sin forma de
aprobarse. El criterio que se retiene, "compañeros de guardia", se
reduce a algo ya computable — que la guardia siga cuadrando entre los
residentes del mismo grupo — que es exactamente el mecanismo que
propone `P-13`. P-10 no introduce una entidad de aprobación propia:
converge con P-13, aplicado a las fechas de Navidad/despedida que ya
cubre INV-10.

**P-6, implementada el 2026-08-06 por decisión directa del autor** (no
se consultó a tutoría — el autor tenía criterio propio y lo usó). La
normativa es ambigua en sí misma («al menos una de las guardias de
viernes y sábado»); el código exigía conjunción (viernes **y** sábado).
Pasa a **disyunción** (viernes **o** sábado): quien rota cerca solo
necesita cubrir uno de los dos días del periodo, no los dos. Confirmado
de paso que la distinción `provincia` (Alicante/colindante activa
INV-7, el resto no) se mantiene sin cambios — rotar en Alicante sigue
contando como disponible para el reparto. `v2/domain/validate.js`
actualizado (ya no acumula `faltan` por día, un único chequeo
"¿cubrió viernes O sábado?"), 3 tests nuevos/reescritos en
`validate.test.js` (los dos sentidos de la disyunción + el caso que
sigue violando cuando no se cubre ninguno de los dos) y el escenario de
severidad de V-14 ajustado para seguir disparando INV-7. Bundle
regenerado (`npm run build`). 612/612 tests en verde.

**P-9, rechazada el 2026-08-01 por decisión directa del autor** (no
había normativa que evaluar: la propia fila ya decía "extensión sin
respaldo normativo, decisión libre del autor"). Horario explícito por
guardia: rechazado — "los residentes ya conocen el horario de memoria,
y no aporta nada que la app no resuelva ya". Retribución (NORMAL/DOBLE):
rechazado — se gestiona fuera de la app (RRHH/nómina), no algo que la
app deba calcular ni exportar.

**P-2, cerrada el 2026-08-01 (no tenía su propia fecha de cierre —
detectada obsoleta al revisar la cola completa).** Proponía tratar
vacaciones y rotación como bloqueo DURO. `V-8` (§6, ya implementada en
Fase 5.x) decidió exactamente lo contrario: BAJA sigue bloqueando
(INV-5 sin cambios), pero VACACIONES y ROTACION dejaron de bloquear la
asignación — la fila de la cola seguía marcada `propuesto` sin que
nadie la hubiera actualizado tras esa decisión. Marcada `rechazado`
porque el comportamiento implementado es el contrario al que propone,
no porque se haya evaluado de nuevo contra la normativa.

**P-1, P-3, P-4 y P-5, cerradas el 2026-08-10 (ninguna tenía su propia
fecha de cierre — detectadas obsoletas al revisar la cola completa, igual
que P-2).** Las cuatro ya traían su columna "Respaldo en `normativa.pdf`"
en contra desde que se registraron, pero nadie había bajado el estado de
`propuesto` a `rechazado` tras esa evaluación — quedaban como si siguieran
esperando decisión cuando la decisión ya estaba escrita en la fila de al
lado. Marcadas `rechazado` sin reabrir el análisis, por la misma razón que
ya está en cada una: **P-1** (índice ponderado de equidad) contradice el
umbral entero «diferencia máxima de 1» de p.1/p.2; **P-3** (redefinir la
mochila del R1 como un tercero) contradice la cita literal de p.3 sobre
quién cubre primero al R1; **P-4** (equidad fuera de la cohorte) contradice
las cinco apariciones de «del mismo año» en la normativa; **P-5** (mochila
de junio a diciembre) contradice que junio-agosto es justamente el tramo
en que la normativa saca a los R1 del reparto (INV-11).

**P-7 y P-8, cerradas ambas el 2026-07-26.** Eran las dos donde el contraste dio la razón a `docs/`
y no a `v2/domain`, y ninguna ha terminado cambiando el modelo de datos:
- **P-8 → implementada** (Fase 7.2, decisiones V-13 y V-14): cierre trimestral de INV-3 con un solo
  eje (`total`) y severidad aviso. De paso se conectó al flujo el cierre anual, que llevaba desde la
  Fase 3 implementado sin que lo invocara nadie.
- **P-7 → rechazada** (decisión V-15): los dos «cuadrantes» de la normativa son la regla de
  composición Pequeño+Mayor (INV-1) y la organización de cada grupo, no dos tablas. El cuadrante
  mensual único, el ciclo de estados y la proyección a Sheets se quedan como están.

### 8.2 Referencias documentales inexistentes

Los documentos de `docs/` citan como fuentes vigentes al menos diez artefactos que **nunca han
existido en este repo** (verificado con `git log --all --diff-filter=A`, no solo con `ls`):
`VACATION_IMPACT_MODEL.md`, `DOMAIN_MODEL.md`, `DOMAIN_DESIGN_CALENDARIO.md`,
`DOMAIN_DESIGN_PLANIFICACION.md`, `FLEXIBLE_COHORTS_REVIEW.md`, `INSTITUTIONAL_CONTINUITY.md`,
`Reglas_de_Negocio_App_Guardias.docx`, un «SRS», un «Diseño Técnico del Motor de Planificación» y un
«Diseño de Base de Datos» — más las entidades `ParametroConfiguracion` y `BalanceEquidadHistorico`,
que se dan por modeladas y no están en las 9 tablas de `server/src/sheets-schema.js`.

Son citas generadas por plausibilidad al redactar con asistencia de IA, no documentos perdidos. El
problema práctico: `EQUITY_SYSTEM.md` construye todo su índice sobre un «RN-24» que nadie puede
leer, así que **esas afirmaciones no son auditables por nadie**, ni hoy ni dentro de diez años.
`test/docs-trazabilidad.test.js` congela la lista como deuda conocida y falla ante cualquier
referencia **nueva** a un fichero que no exista.
