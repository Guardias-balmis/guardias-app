{
  "name": "guardias-app",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node build/build-gas.mjs",
    "test": "node --test 'v2/domain/test/*.test.js'"
  }
}
